# Deep-phase queue — priorities from the first full-roster report

- Type: wayfinder:task (backlog record — each item becomes its own ticket after a design
  session with Henry; nothing here is pre-authorized for implementation)
- Status: **open**
- Blocked by: tickets 51 (cleanse→shed) and 49 (floor pass re-read) remain queued ahead per
  Henry's earlier sequencing; the NOURISH unit fix (policy item 3 in HANDOFF) can ride with
  the Light polish below.

Measured at registryHash `1:e2f392b8` (first report with all 32 decks live) plus the
follow-up field round robin. Read under the DEEP-PHASE POLICY at the top of HANDOFF.md:
field + control are primary; §2.3 is a diagnostic only; all design changes go through Henry.

## Queue (order Henry-approved 2026-08-12)

1. **jormungandr — species rebuild + Water re-price sweep.** v1: 25% field, loses ~70% to
   the frozen control (under the floor). v2: 9% field, dead-last, contagion 82% dead with
   an 11.8x power divergence, capacitor 73% dead. Fold the systematic Water over-band drift
   into the same design session: surge_protection 4.0/3.0, ink_stream 3.6, serpents_coil
   3.8, pressure_point 3.1 — band drift from the curve revisions, one sweep not four fixes.
2. **Light polish — NOURISH unit fix (approved, HANDOFF policy 3) + valkyrie_v2 trim.**
   v2 is the field's #1 at ~94%; printed offenders falling_star 3.6/3.0 and glimmer 1.4/1.0.
   audhumbla_v2 comes off 24% field once heals convert in power units.
3. **hel_v2 — blood-mage pass.** 28% field, the only deck the control still beats outright,
   30% dead, owns the roster's only FTKs. Suspect UNDERWORLD_GATEWAY economics (HP-paid
   casts need hand-priced payoffs, 8c4), not card knobs.
4. **Fire pair — skoll_v2 + fenrir.** skoll_v2 26% field + the open scorch re-cost;
   fenrir_v1/v2 28% field, ash_communion simultaneously over-budget (7.10/6.5) and 71%
   dead — a design smell, Henry decides the card's fate.
5. **kraken_v2 — 27% field, 0.53 vs control.** (Note: its byte-identical history across
   tickets is expected determinism — unchanged matchups cannot drift — not extra evidence.)
6. **Winners' dead-card cleanup — hraesvelgr_v2 (86% field DESPITE 48% dead: tailwind 79%,
   cinder_gust 58% — its true ceiling is hidden), ymir_v2 (85% field, 36% dead).** Also
   watch: nidhoggr_v1 83% field.

Card redlines to fold into their owners' passes: benediction 3.1/3.0, glimmer 1.4,
falling_star 3.6, dawn_of_creation 10.9/10.5 (Light polish); ink_stream (Water sweep);
lumen_surge 4.5/3.0 (touches Dark — its own small ticket). Report-tool chore: a suppression
list for documented exceptions (water_slap 1.2 appears 32 times and buries real diffs).
