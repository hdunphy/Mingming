# HANDOFF — deck-archetypes map (keep this current every session)

*Last updated: 2026-08-15, after ticket 64 (skoll's deck pass) landed. **Both skoll decks are off the floor and ticket 64 is CLOSED GREEN — Amendment 1's curve swap took skoll_v2's dead cards 36.9/38.2 -> 32.5/32.8, under the gate on both seed bases.** Field now v1 47.7/48.1, v2 44.9/41.5. Queue next: repricing (66) -> kraken (65) -> hel_v1 -> hraesvelgr; ticket 63 census in any gap.*

## DESIGN DECISIONS (Henry, 2026-08-16 - answers to ticket 67; the design agent is out of tokens, so this session designs AND implements)

1. **Kraken picks a lane.** Sweep HP, then ATK, then DEF - "with low HP and Def it's not working". Ticket 70; the implementing session sweeps, picks and ships.
2. **THE CURRENT TYPE IDENTITY IS REJECTED.** "We don't want type matchups to be an automatic loss, so even her win rate vs fire is not good. The neutral should be more deck dependent. There might be some counters but not all of them should counter Kraken." **Both ends are the problem - a 100% win is as unacceptable as a 0%.** Scope decision: MEASURE FIRST (ticket 69) before touching the elemental system. Note `combatUtils.ts`'s own standing instruction: shaving 1.5 was already measured not to work (1.05/1.0 still produced an 89/11 split); if it moves, **change the mechanism's SHAPE**.
3. **`capacitor` ramp STAYS.** Ticket 67 measured removing it makes kraken_v2 worse - it buys her 3-energy payoff turns.
4. **`surge_protection` fires only on a TRIGGERED draw** - a card, OS or daemon. "This effect is broken if it triggers when you get a new hand." Shipped as ticket 68, and it found two stacked defects.
5. **NEW STANDARD: 10-90% single-deck matchup bands.** "No single matchup should win >90% of matches." High sustain beating kraken is acceptable; 100% is not. **This is a per-MATCHUP band, not an aggregate** - every existing gate is an aggregate and none of them can see a 100% cell. Census in ticket 69.

## META-LAYER DECIDED (Henry, 2026-08-16, corrected same day): the shipped game is 3v3, SIMULTANEOUS 3-ACTIVE, **NO SWITCHING**

- **CORRECTION (Henry): NO switching - it adds too many variables.** A fight is all six on the field, targeting matters, allies cover countered members IN-FIGHT (matches the engine's existing side/ally machinery: RANDOM_ENEMY, whole-side effects, TREACHERY's ally trigger, ticket 05). The tactical layer is TEAM CONSTRUCTION: mono-element for full STAB vs element-spread matched to deck type vs coverage-spread (good everywhere, great nowhere). Implications: on-switch-punish design space never existed; setup decks keep full value; the bucket-band standard stands exactly as adopted (simultaneous coverage is what makes typed unwinnable 1v1 cells acceptable). Sequential-1v1s explicitly REJECTED.
- **The type chart stays x1.5, unreshaped.** The 95 typed band violations RECLASSIFY as the strategic layer (a countered lead switches out - Pokemon-shape). The elemental pivot question is CLOSED: no typeless pivot, no multiplier reshape.
- **BUCKET-BAND STANDARD adopted:** neutral-type cells are the ONLY balance bugs. Near-term hard gate: NO absolute 0%/100% in any NEUTRAL cell. Direction: neutral cells inside 10-90. Typed cells exempt by design. The band census (ticket 69's instrument) joins the standing gate suite (ticket 73 task 3). Within neutral, the ARCHETYPE WEB (research/archetype-web.md) licenses designed soft counters at 65-80% - lopsided-by-role is texture, lopsided-by-accident is a bug.
- **Resurrections:** ally-heal firmware space, ink_cloud, einherjar_standard, ticket 05 team scenarios - all live again. **Team-battle simulator is the roadmap horizon after the neutral-spread cleanup** (the entire suite is 1v1 today; 1v1 deck quality remains the foundation, but switching re-values setup decks and opens on-switch-punish design space).
- Queue (2026-08-16): **73 FTK correctness (FIRST - hard-gate integrity) -> 72 kraken web package -> jormungandr_v1 cut (design session with Henry: 90.0% mean, 11 cells >90, strengthened by ticket 71 - power problem at ANY type philosophy) -> hel_v1 -> hraesvelgr pass (multi-base first-mover re-read) -> dead-card cleanup -> post-queue re-census** (judges jorm accept-and-watch + hel_v2 waiver + the neutral-band count).
- Ticket-70 resolution reviewed and ratified (ATK 80->100): v1 in band at 43.2 with neutral zeros -> 0; v2 stopped at 27.9 -> ticket 72 is her tools pass. Anti-zoo shape decision: Henry's PUNISH DAEMON (riptide - per-turn threshold 3, cumulative-5 measured as fallback arm e) over the ink-veil (poison hole + new-status cost; veil PARKED as fallback design).

## DEEP-PHASE POLICY (Henry, 2026-08-12 — supersedes the band rules below where they conflict)

1. **§2.3 IS DEMOTED TO A DIAGNOSTIC.** Two OSes of one species MAY hard-counter each other — that is
   allowed design, not a defect (the ratatoskr lesson, 8-COUNTER, is now policy). The PRIMARY
   instruments are: **field win rate** (round robin vs the tuned roster; healthy window ~0.35–0.80)
   and the **control floor** (deck should beat the frozen control ≥0.60; the control NEVER moves —
   its aggregate drift is telemetry, not decalibration). Dead cards ≤0.35/side, FTK 0, and
   mirror-decides still gate. A §2.3 extreme prompts a field check FIRST, never a direct fix.
2. **HENRY IS IN EVERY DESIGN DECISION.** Implementation agents may adjust ONLY numeric knobs from a
   Henry-approved list in their prompt, max two rounds. Any card/effect/deck/OS change beyond that —
   including "trying" a swap to see what happens — STOPS the ticket and returns findings for a design
   session with Henry. Do not just try stuff.
3. NOURISH_ROUTINE unit fix (Henry-approved): convert from PRINTED POWER at the damage exchange rate,
   dial at **50%** (a 90-power heal also strikes for 45 power-equivalent ≈ 13 HP), floor 1 per heal.
   The HP-denominated version rounded a third of the deck to zero.

### Ticket-55 review decisions (Henry, 2026-08-12)

- **Gates finalized: field 0.35-0.80, control >=0.60** (supersedes any 0.75 reference).
- **jorm_v1 at 83.0 field: ACCEPT-AND-WATCH** - within +-4-5 noise of the ceiling, and the field re-reads after ticket 56 moves valkyrie_v2/audhumbla_v2. No new lever authorized; re-read then.
- **huldra_v2 `bark_start`: DELETE as dead code.** Flipping the grant to turn-START would be a real BUFF per 8-SHIELD-TIMING on a healthy 71%-field deck - recorded instead as a ready-made buff lever if huldra_v2 ever needs one.
- **STANDING POLICY: run `liveness.ts` after every hooks.json edit** - the only instrument that sees a dead hook. Three dropped-action occurrences (GENESIS, OUROBOROS, jorm_v1_reset) are the confirmed complete set as of `1:53ea4a83`.
- Authorized measurements (report-only): roster-wide first-mover census; TOXIN_FANG 2->1/2->3 A/B with contagion dead-rate; 30-iteration field census.
- Queue drift per the floor re-read: kraken_v2 and fenrir_v1 are OFF the floor list (surge_protection side effect); hel_v2 (0.901 control-wins) is the clear head; ticket 56 then 57 are specified and next.
- **Tickets 58 + 59 specified and open (2026-08-13), runnable in PARALLEL with 56/57** (one is read-only, the other docs-only): **58 = Fire investigation** (wayfinder:research, REPORT-ONLY; quality bar = research/jormungandr-v1-attribution.md; Henry designs Fire only after its report comes back); **59 = registry triage** (annotation-only pass over research/registry-inventory.md, sized for a lower-tier model; deletions are a later ticket after Henry reviews). Pricing question from Henry answered in-session, no repo change needed: ticket 51 already shipped his removal-costs-more rule as REMOVAL_PREMIUM 1.25; no Str/Sharp reprice (duality premium ~2 power/stack, inside rounding); shrug_off IS the legal 0e pure shed at 1.00; purify sits at 2.70 in 2.4-3.0, so a heal rider does not fit without shedding less - his call, parked.
- **Census decisions (Henry, 2026-08-13):** (a) **TOXIN_FANG `bonus` 2->1 APPROVED** - ticket 55 knob round 1 of 2, applied NOW rather than post-56 (30-iter reads 84.0/84.2 are decisively over the ceiling; the knob's direction cannot be flipped by the ~2-3 pt field shift 56 brings). Implementation prompt staged for the active agent - land it BEFORE 56's full-balance re-baseline. Round 2 stays banked; Contagion's residual ~0.44 dead rate belongs to the dead-card cleanup queue item, not a knob. (b) **|first-mover edge| > 20% ADOPTED as a DIAGNOSTIC FLAG only** - flags hraesvelgr (-33.0%) and skoll (+24.5%) today; never a tuning gate, never spends knob rounds; revisit the threshold once ticket 58 explains skoll's sign. (c) draugr_v2 (31.3) and fafnir_v1 (32.0) are within noise of the 0.35 floor: WATCH, do not tune.
- **DESIGN HOLD (Henry, 2026-08-13): no new design thread opens until 56/57/58/59 report back.** The next card-design session is FIRE, with ticket 58's report in hand. After Fire: kraken (both decks below the field window at 25.1/26.8 but fundamentally healthy - beats control 0.76/0.85, dead ~0.10, FTK 0, mirror 5.2 turns - so it needs a small per-opponent diagnostic ticket, not a rebuild reflex). hel's drain-suite swap is designed ONLY if ticket 57's sustain finding demands it.
- **Ticket 62 Amendment 1 (Henry, 2026-08-14): direction NOT picked - full grid first.** The single-shape ship is superseded: 21 arms (DETONATE-modulo vs VENT x cap {3,4,5} with spread tiers keeping the 8%/5%-shred top x D {4,6,8}, + two tick arms + baseline), REPORT-ONLY then STOP for Henry's pick. Symmetric self-burn stands in every arm. Spread tiers for C=4: 1.5/3/5/8; C=5: 1.5/2.5/4/6/8 (shred scaled alongside). Grid still runs AFTER ticket 61.
- **Ticket-61 close-out (Henry, 2026-08-14): valkyrie_v2 DONE at N=15 (59.1/64.6 two-base).** Report Q answers: (1) once-per-turn cap stays; **Henry leans toward REMOVING the cap in a future pass for readability/fun - it is the designated BUFF lever (+66% OS output, ticket 60); N stays the nerf dial; never both at once.** (2) os:valkyrie 0.89 accepted as counter-texture (ratatoskr precedent). (3) power-rate census -> ticket 63 (open). (4) starfall stays: 6.7/cast sits in the rider band (6.0-6.8) with no rider vs vanilla fire_punch_v2 at 10.5-13.5 - low, but a buff re-OPs her (+15-20 HP/game at 15/draw); judge against ticket 63's full scaler table. SEED-BASE LAW (extends 0-DECISION-GRADE): two independent 30-iter reads differed 5.5 pts (59.1 vs 64.6); any decision within ~6 of a band edge needs MULTIPLE seed bases.
- **Ticket-64 review (Henry, 2026-08-15): skoll SHIPPED, one gate open -> Amendment 1.** Both decks in window for the first time since ticket 13 (v1 45.6/46.1, v2 51.0/50.1 two-base); first-mover flag CLEARED (v1 +18.3/+3.3, v2 -7.1/-12.1 - and the 15-pt v1 spread means NO single first-mover reading is a number; hraesvelgr's -33% gets multi-base re-measurement in her pass before anyone designs against it). Open gate: v2 dead 36.9/38.2 vs 0.35 - **Amendment 1 approved: overdrive x2->x1, fury_strike x1->x2** (the only 1e attack that feeds the OS), no knobs, STOP if it misses. **Str-consumed constant -> 8 in ticket 66** (measured 7.91/8 median, supersedes the 5). **os:skoll 0.30 = texture, ratatoskr precedent (third application).** **FIRMWARE x DAEMON COMPOUNDING IS BY DESIGN (Henry)** - roguelike jackpot, bounded by daemon cost + both caps; recorded as a pool watch-item with a MANDATORY revisit at the pre-Steam player-deckbuilding degeneracy audit. Round-2's negative result stands recorded: power dials do not move dead-card rates.
- **Kraken lockout check authorized (Henry, 2026-08-16) -> ticket 67 (open):** before the kraken design session, per-zero-matchup verdicts (STRUCTURAL / POWER-GAP / WINNABLE-MISPLAYED) via throughput-ceiling-vs-sustain-floor, loss autopsy, near-win margins, and in-memory pool probes (+Poison pair, -capacitor, both); plus the surge_protection draw-phase-condition grep (suspected conditionless everywhere - 100% uptake in 3,371 casts). The design session (Henry + designer) runs on its verdict table. Tickets 64 (gate closed 32.5/32.8), 65, 66 all reviewed 2026-08-16; contagion 20.4-and-44%-dead noted as a package for dead-card cleanup.
- **Census review (Henry, 2026-08-15) -> tickets 65 + 66 (open, 7ac1c0f):** **Ticket 66 extends the shipped consumed-table: Poison 8 (conservative end of 11.47-mean/tail), Strengthened 5 (pre-seeded for sun_devourer vs TREACHERY's 4.8), DISTINCT_STATUS 1, WEAKENED 5, BARKSHIELD 7, DAZED stays 3 (delete the ~10 comment), MULTIPLY_STATUS per-status, plus the molten_core accumulated-pile model fix.** Burn-scorer modernization struck from the ticket - already shipped (0-BURN-PRICE-LAG closed). **POLICY (Henry): repricing triggers NO card changes in either direction** - new redlines (expect wither_feast/umbral_feast/contagion/heat_wave, molten_core ~1.9 over) are documented as deliberate; the sanctioned deck-level buff lever afterward is raising printed status counts, enabler-first. **Ticket 65 (kraken diagnostic, report-only, lower-tier):** per-opponent 8-DIFF, damage/turn vs roster norm, TIDAL_CRUSH/P2/surge_protection delivery, detonation non-interaction check; report decides knob vs design session. Queue: 64 skoll -> 66 -> 65 -> hel_v1 -> hraesvelgr; 63 census in any gap.
- **Burn-grid review (Henry, 2026-08-15) -> ticket 62 Amendment 2:** **DETONATE is the chosen shape at the price it needs - mini-sweep D {10,12,14,16}% at cap 3** (extrapolation: ~40/44/48 field for fenrir_v2 at 12/14/16). **skoll_v2 REMOVED from the Burn decision and queued for her OWN deck-revamp design session** (Burn is 18% of her damage; fire_punch_v2 carries both her decks; adrenaline 57.8% dead, daemon 42.5% dead, TREACHERY overfeeds the 8-cap in 57.5% of games, +24.5% first-mover - all of that is her ticket). **hraesvelgr_v2 REMOVED too and queued for her own pass** (Air, 79.7 live pre-existing, -33% first-mover). VENT-C4-D8 is the measured fallback if the sweep misses. Queue after Burn ships: skoll revamp -> kraken -> hel_v1 -> hraesvelgr pass; ticket 63 census fits any gap.
- **Ticket-58 review decisions (Henry, 2026-08-14) -> ticket 62 (open):** Burn reworked to **DETONATION with modulo carry** - cap-crossing pays D% maxHp to the burned entity, stacks reset by 3s (rebuild rhythm bounds detonation rate; the old 0.08-per-excess-stack design lacked exactly this and was measured OP - see BurnBehavior's comment block). **SYMMETRIC by Henry's ruling: self-Burn detonates on yourself** - pyre_sacrifice is a managed bomb, ash_communion becomes the release valve with NO text change. Sweep D {4,6,8}%, ship per ticket 62's rule, run AFTER 61. Cap-and-vent variant REJECTED (it is the measured-OP design at a lower dial). Tick tiers/decay/cap-3 untouched round 1. **Deferred to the post-detonation Fire session:** ash_communion scorer (ASSUMED_STATUS_COUNT 3 vs measured 1.5 stacks), TREACHERY 8-stack scaler waste (57.5% of games, daemon 42.5% dead), fire_punch_v2 carrying both skoll decks (Fire identity question), skoll +24.5% first-mover. **Process (designer's call): floor-list membership is decided at 150 iterations from now on** - fenrir_v1's 0.29->0.194 'recovery' was seed spread (0.101 across five bases straddling the 0.25 line); its floor membership is UNKNOWN pending a 150-iter read.
- **Ticket-60 review decisions (Henry, 2026-08-14) -> ticket 61 (open):** the knockout study named the engine - REBIRTH is 50 of valkyrie_v2's 88 points, and the root cause is a UNIT BUG: the damage half is a raw HP mutation (~33 power/proc free, level-broken flat damage - the hel denomination law violated). **Fix approved: power-denominate BOTH halves via the NOURISH ATTACK-pipeline pattern, sweep N in {10,15,20,25}, ship nearest 0.60 field with 30-iter confirm.** glimmer restored (8 cards, the measured arm) and the 0-DECK-SIZE-EXCEPTION test line REVERSES. Rejected with numbers: +1-energy reward (Energized ~35 power/stack = a lateral, not a nerf; GENESIS identity collision) and raw 0-10 sweep (stays level-broken). Shuffle-throttle held as second axis, Henry sign-off required. starfall stays - re-read at dead-card cleanup, post-fix games lengthen. Both prior hypotheses (exhaust package, starfall mispricing) were measured WRONG - deck size and individual cards are NOT levers (all arms 87-91). Knockout-report erratum: its "~33 healing" claim is wrong, the heal half is power-denominated (~7 HP/game).
- **Ticket 61 LANDED (2026-08-14), shipped N = 15** ([research/valkyrie-rebirth-fix.md](research/valkyrie-rebirth-fix.md)): field **86.7 -> 59.1** (30-iter; second seed base 64.6), control 1.000, FTK 0, dead 0.078, v2 mirror 100% decided at 8.90 turns, deck back to 8 with the 0-DECK-SIZE-EXCEPTION line REVERSED. **The sweep's general product: ~0.19 HP of damage per printed power at level 15 and ~0.20 HP of healing per power** - so the ticket's own guide was off ~40% and the raw `-10` was worth **42-45 power a proc**, not 33. **The nerf partly pays for itself**: procs/game rose 3.48 -> 4.05 because field games lengthened 4.88 -> 5.80 turns, which makes the banked **shuffle-throttle a BIGGER lever than ticket 60 measured**. `starfall` 6.7 dmg/cast at 4.02 casts (from 7.1 / 3.11); her dead ratio halved to 7.8%. **Returned for Henry, unacted: `os:valkyrie` re-opened at 0.89 while the FIELD says the opposite** - v1 55.0 / v2 64.6 on one seed base, both in window, both beating the control - the cleanest 8-COUNTER case yet. 8-DIFF: two rows moved, 65 bit-identical. Redlines 48 -> 49; registry `1:8b7b0ae9`.
- **Ticket 62 GRID DELIVERED (2026-08-14), STOPPED - nothing shipped** ([research/burn-grid.md](research/burn-grid.md)): 21 arms at 10 iterations, seven leaders re-read at 30. **The waste question is CLOSED - unpaid stacks 40.4% -> 0.0% at every D in every arm.** **The balance question is not: no arm puts both Fire decks in 0.35-0.80 with hraesvelgr_v2 under the ceiling.** Closest at 30 iters `VENT-C4-D8`: fenrir 79.2 / skoll 38.7 / hraesvelgr 80.1 - misses by 0.1. **Shape is worth ~44 field points** (VENT 78.3 vs DET 34.6 at C3/D8) and **DETONATE SPENDS the pile** (fenrir tick 24.3 -> 19.8 HP/game) - a trade of DoT for burst, which is why **every cap-3 DETONATE arm puts skoll BELOW baseline**. **Burn is NOT skoll_v2's lever** (15.1-39.0 across 21 arms, beats baseline only in arms that send fenrir to 66-82) - she is a card problem. **fenrir_v2 is entirely steerable, 27.6 -> 79.2 on one dimension.** **Cap is the only lever that lowers hraesvelgr** (79.7 live, at the ceiling already). Tick +-2pp = -7.1/+1.4 fenrir, -1.6/+5.6 skoll. **Symmetric self-burn costs fenrir <=0.95 HP/game - free everywhere.** draugr sentinel held (0 events). FTK 1 of ~21,000, in the lowest-payout arm. Committed: refactor only (`BURN_CONFIG`, both shapes, 15 tests), scoped fenrir run reproduces committed numbers EXACTLY. Five questions returned; the load-bearing one is which constraint gives.
- **Amendment 2 DEEP SWEEP delivered (2026-08-15), HELD not shipped** ([research/burn-detonate-deep-sweep.md](research/burn-detonate-deep-sweep.md)): Henry widened amendment 2 in session to **DETONATE cap 3-8 x D 6-16%**, 36 arms + baseline, ~46,000 games. **FTK 0 everywhere** (largest burst 14 HP) - the burst-FTK watch item is clean. **Nearest 0.50: `DET-C4-D14` at 48.5%** (two seed bases 49.4/47.5); within the cap-3 constraint it is `DET-C3-D12` at 47.6%. **Same fenrir number, different collateral: C3 costs skoll_v2 6.4 pts and puts hraesvelgr_v2 at 83.0 (over ceiling); C4 leaves skoll +1.8 and brings hraesvelgr to 77.7 (under).** The cap-4 tier table is amendment 1's verbatim, so it opens no new design surface. See 0-BURN-CAP for the structural finding. **Amendment 2's ship-and-gate section is keyed to a cap-3 arm - if the answer is cap 4 it needs an amendment before the agent runs it. Nothing shipped; five questions returned, the load-bearing one is cap 3 or cap 4.**
- **TICKET 62 SHIPPED (2026-08-15): `DET-C4-D14`** - Henry picked cap 4 off the deep sweep. Burn caps at **4** on the four-tier spread table (1.5/3/5/8, shred 0/1/2.5/5) and crossing it **detonates for 14% maxHP with modulo carry**, symmetric. fenrir_v2 **24.9 -> 48.5 field**, control **55.0 -> 93.0**. **FTK 0 everywhere** (67 matchups + 5,400 verify + 46k sweep; largest burst 14 HP). 8-DIFF **10 of 67 moved, 57 bit-identical** - only the four Burn species and their control aggregates; no control-vs-non-Burn row moved. Suite 797/60, tsc + build clean, redlines 49 -> 50, registry unchanged `1:8b7b0ae9` (engine constants only). **The on-disk build was VERIFIED against the measured arm, not assumed equal** - re-read reproduces 49.4/47.5 exactly. **THREE ITEMS RETURNED: (1) LOAD-BEARING - `powerscale.ts` still prices Burn off the OLD 3-tier table and prices overflow per-excess-stack at a value derived when it floored to 0 damage; every Burn card is now mispriced and section 1.3 is measuring a mechanic that no longer exists. Proposed carry-forward `[4.5, 13.5, 28.5, 52.5]` + 42/detonation is ALSO a shape change (floor(S/cap) events, not S-cap), so it moves card budgets and is Henry's call - full working in power_curve_spec.md's rev note. (2) skoll_v2's control floor came UP 39.0 -> 52.0, still under 0.60 - improved by the mechanic the grid said could not help her. (3) `os:fenrir` opened at 0.419 (v2 beats v1 91.9%), diagnostic-only but fenrir_v1 is a floor-queue deck.**
- **Burn REPRICED (2026-08-15)** ([research/burn-repricing.md](research/burn-repricing.md)): §1.3 holds at **40** redlines with membership changed (`sun_eaters_plunge` OFF 10.8->9.7, `ash_reclamation` ON 3.0->3.2, `ash_communion` 10.6->9.3 and **still 2.8 over**); §2-3 byte-identical. **Every Burn card in every deck got CHEAPER or flat - none more expensive** - because the spread table lowered the middle rungs and **no card applies 4 Burn in one action**, so **the detonation is priced but unreachable by a static per-action pass**. **`molten_core` is materially mis-modelled** (2+2 priced as 27, engine pile of 4 = 52.5; under by 2.55 on a 3.0 budget, so honestly modelled it is 1.9 OVER) - pre-existing independence, newly material because the table is non-linear across the cap; same card ticket 58 saw wasting 64%. **`ash_communion` is an ASSUMED_STATUS_COUNT problem, not a Burn-table one.** See 0-FRACTIONAL-STACKS. **A STACK COUNT CAN BE FRACTIONAL, AND ONE SCORER PATH INDEXED AN ARRAY WITH IT (2026-08-15).** `ASSUMED_STATUS_COUNT` is 1.5, and `burnPower` read `BURN_TIER_POWER[stacks - 1]` - index 0.5, `undefined`, **a silent NaN into the card score rather than a wrong number**. Burn was the only path that broke because it is the only one that INDEXES: `poisonPower`, `regenPower` and `streamStacks` are formulas and take fractions fine. Fixed by interpolating between rungs, which is also the honest reading of the constant (half the time 1 stack at 4.5, half the time 2 at 13.5, so 1.5 -> 9.0). **Any future tier-style lookup keyed on a stack count must assume the count is fractional.**

0-BURN-NONMONOTONIC. Four questions returned.
- **ASSUMED_STATUS_COUNT 3 -> 1.5 (2026-08-15, Henry)** ([research/assumed-stacks.md](research/assumed-stacks.md)): §1.3 **40 -> 37**, §2-3 byte-identical. Off the list: `ash_communion` 9.3->4.1 (the target), `rimebreaker` 7.5->3.8, `ash_reclamation` 3.2->2.1. Ten cards moved in total; nine down. **See 0-ASSUMED-STACKS for the blast radius - the constant serves seven paths and only one was measured, so the pricing error changed SIGN rather than shrinking; `hexbloom` went 6.3 -> 1.8 against a 6.5 band.** Also fixed en route: **`burnPower` indexed its tier array by stack count, so a fractional count returned `undefined` and propagated a silent NaN** - it now interpolates between rungs (1.5 stacks = 9.0 power), with four regression tests. 818 tests / 61 files green.
- **CORRECTED same day: the 1.5 is BURN-ONLY** ([research/status-pile-census.md](research/status-pile-census.md)). Henry: "this should have only been for burn statuses." `ASSUMED_STATUS_COUNT` back to 3, `ASSUMED_CONSUMED_STACKS = { Burn: 1.5 }`, and STATUS_CONSUMED heals resolve their status from the card's consume action. **§1.3 = 38** (`ash_communion` + `ash_reclamation` off; `rimebreaker` and `hexbloom` restored). **And the census Henry asked for now exists - 3,840 battles - see 0-ASSUMED-STACKS: 3 had never been measured and is wrong for four of six paths in both directions.**
- **Ticket-57 review decisions (Henry, 2026-08-13):** (a) **Gateway cap REVERTS to 20%** (ticket 57 Amendment 1) - texture over the number; field ceiling consciously waived at 81.6-provisional; BOTH knob rounds now spent, further hel_v2 change is a design session. Agent's 20%+cost-10 combo rejected on arithmetic (soul_tithe 30% > 20% cap = uncastable). (b) **valkyrie_v2 7-card test exception RATIFIED as TEMPORARY** - reverses when ticket 60's fix restores >=8 cards. (c) **hel_v1 (26.7 field) joins the floor queue AFTER kraken**; aim-upward applies; do NOT chase parity with v2 (os:hel 0.030 is diagnostic-only, hard counters legal). (d) Agent order: 57 Amendment 1 first, then ticket 60.
- **CEILING FREEZE (Henry, 2026-08-13, standing policy):** no tuning on any deck above 0.80 field until the floor queue (Fire, kraken, hel_v1, residuals) lands. Field is zero-sum: the remaining floor passes redistribute ~100 deck-points of win-mass, mostly out of the top farmers - a ceiling deck holding five ~0.90 floor matchups loses ~10 field points when they become 0.60 fights, so every ceiling read is PROVISIONAL while the floor moves. Exceptions (diagnosis and law, not tuning): FTK, rulebook violations (valkyrie_v2's 7-card list), and engines named by measurement (ticket 60 runs). Re-census after the queue, then judge the ceiling - the top cluster (valkyrie_v2 84.7 / jorm_v1 84.0 / hel_v2 78.2) reads TOGETHER at that census.
- **AIM FLOOR PASSES UPWARD (Henry, 2026-08-13, standing policy):** every remaining floor design takes "what does this do against the top 5" as an explicit design input - counter-texture inside decks being fixed anyway (fast pressure punishes valkyrie_v2's 13.6-turn setup and ymir_v1's 14.1-turn wall; defense-ignoring DoT punishes ymir - Fire's natural aim). Hard counters stay legal as texture (ratatoskr precedent) but are NEVER the fix for an overperformer: a roster counter is the least pool-proof lever there is - it patches the matchup table while leaving a mispriced engine intact for player deckbuilding.
- **Tickets-55/56 review decisions (Henry, 2026-08-13):** valkyrie_v2 at 84.7 / 7 cards is REJECTED as an end state - the rulebook floor is 8 and the fix will restore >=8; **ticket 60 (valkyrie knockout study, report-only) is the authorized next step** - run it AFTER ticket 57, one worker per tree. Two engine hypotheses on record, neither measured: agent's exhaust-package-feeds-OS vs designer's starfall-real-trigger-count (serpents_coil lesson). os:audhumbla 1.000 LEFT ALONE per deep-phase policy (both decks in window, both beat control). min-1 NOURISH floor stays unbuilt, documented. HEAL_INTENDED dead scaling folded into ticket 59's triage scope; liveness-for-scalings extension authorized as a small task. jorm knob round 2 still banked.

## Read this first: which band applies

There are TWO bands and they are not the same bar.

- **First-pass band, 0.30–0.70 §2.3.** This is the working bar while every species gets its initial pass. **Use this one.**
- **Strict ±15% (0.35–0.65).** The auditor's own `osMaxGap` assertion in `balanceReport.ts`. It will redline species that are fine by first-pass standards. **Henry's instruction: ignore it for now — it is for the final tuning pass once all elements are in.** Do not spend knob rounds chasing it.

Dead cards ≤0.35 **per side**, FTK 0, and mirror ≤30 turns still apply at first pass.

## Where things stand

- Branch **card-dev**. Tickets 01–53 closed. **EVERY ELEMENT IS COMPLETE — 32/32 decks live, 16 of 16 species tuned.** There are no placeholder decks left; every number in `balance_report.json` is now balance signal.
- **Ticket 47 changed what "passes" means.** Every first-pass band is same-species or v1-only, and the gauntlet now shows **eight of sixteen slot-2 decks at or below the control floor** — hel_v2 among them at 0.94. **A species is not tuned until BOTH its decks have met the floor.** See 8-GAUNTLET-b for the list.
- **Hel (ticket 36) passes every first-pass band** after a second pass that added `escalatePerPlay`. Read her §2.3 as **±8, not ±4**: 61% of her games are mutual kills, so 0.590 is computed over ~40 decided games. **Her FTK is reduced, not eliminated** — ~1.6% over 800 games, 0 in the committed 100-game sample.
- **Latest full gate (after ticket 34), tuned species only — every one passes, FTK 0, mirror mean 6.0 turns (inside the 5-6 target for the first time):**

  | species | §2.3 | mirror turns | dead cards |
  |---|---|---|---|
  | huldra | 0.470 | 11.7 | 1.0% |
  | sköll | 0.640 | 3.7 | 32.3% |
  | ratatoskr | 0.510 | 4.6 | 4.3% | *(ticket 47)* |
  | kraken | 0.540 | 5.1 | 10.1% |
  | fenrir | 0.394 | 5.2 | 25.4% |
  | jormungandr | **0.240** | 6.4 | 5.2% | OUT OF BAND |
  | sleipnir | 0.330 | 4.5 | 14.7% |
  | hraesvelgr | 0.310 | 3.2 | 4.0% |
  | nidhoggr | 0.320 | 4.5 | 12.9% |
  | draugr | 0.340 | 6.3 | *(ticket 51)* |
  | ymir | 0.620 | 14.1 | 11.0% / 27.7% | *(ticket 50)* |
  | fafnir | 0.596 | 6.7 | 23.6% / 18.0% | *(ticket 52)* |
  | gullinbursti | 0.490 | 10.1 | 10.1% / 6.3% | *(ticket 52)* |
  | valkyrie | **0.170** | 13.6 | 2.2% / 11.0% | *(ticket 53)* OUT OF BAND |
  | audhumbla | **0.000** | 13.1 | 8.3% / 2.1% | *(ticket 53)* OUT OF BAND |

  Hel (tickets 36+38, at `1:d7238b5d`): §2.3 **0.508**, mirror **5.4** turns at 400/400, dead 26.7% / 10.2%. ratatoskr moved 0.590 → **0.610** in the same run (inside noise).

- **THE CURVE IS rev 3.9: `10 / 30 / 65 / 105`, bands `1.0 / 3.0 / 6.5 / 10.5`.** The constants have not moved since rev 3.3 — rev 3.4 is five *pricing* corrections, all documented in `power_curve_spec.md`. **`powerscale.ts` is the executable truth; where the prose disagrees, powerscale wins.**
- **Cards STAY on-curve — fix enablers (OS/deck structure), never bend card economics.** Henry's law, proven three times now (kraken OS decomposition, MOMENTUM_DRIVE, and Burn overflow).

## Three engine/AI bugs found in ticket 28 — do not re-derive these

1. **Lifesteal cards attacked their own Mingming.** `TacticalAI` bucketed any card containing a `HEAL` action as ally-targeting, so `crimson_draw`, `blood_rite`, `leech_strike`, `drain_life` were played with `targetId === sourceId` and their TARGET-scoped ATTACK landed on the caster. Fixed. If you see a lifesteal card measuring 0 damage, this is not a balance number — check targeting first.
2. **The card-advantage eval term double-charged every play.** The search books a card's effect in the leaf state AND books −1 card, so a play only beat ending the turn if it beat the *stock* value of the card it spent — a 7.5-point toll on a 75 HP frame, which made every sub-4-damage card in the registry worse than passing. Fixed by counting cards cast this turn as still held.
3. **Burn overflow dealt a full 3-stack turn per excess stack, instantly, bypassing defense.** `molten_core` was a 1-energy card worth up to 18 damage (24% of a pool) while scoring 2.60 against a 3.00 cap — static analysis cannot know the target already holds stacks. Repriced 0.08 → **0.01** of maxHP per overflow stack. Note `floor(75 × 0.01) = 0`, so overflow is a **no-op at today's 75–79 HP pools** and only starts biting at 100+ max HP. Intentional headroom; `Math.max(1, ...)` is the one-line change if it should always cost something.

## Measurement facts a fresh session needs

- **Per-card damage attribution misses damage-over-time.** The harness measures the HP delta across a single `PLAY_PROGRAM` dispatch, so Burn ticks (which resolve at end of turn) are attributed to nothing. A Burn card will read `0.0 dmg/play` and look dead when it is carrying the deck. Always compare `dmg/game from cards` against `HP/game taken` — the residual is the DoT.
- **The scoped run prints the PLAYER-side dead-card figure only.** A v2 breach can hide behind an in-band v1 number. This has bitten once already (sleipnir was reported in band at 0.384 vs a 0.35 ceiling).
- Scoped run: `BALANCE_ONLY=<species> npm run balance` — 20–60s, never writes `docs/balance/`. Full run ≈ 15–18 min and is the commit gate.
- Measured conversions: **damage = 0.30 × power**; a pool ≈ 79 HP ≈ 263 power; turns ≈ `263 / power_per_turn × 0.90`.
- §2.3 noise ±4 pts at 150 seeds, ±2.8 at 300.

## Two curve rules proved earlier, still binding

- **An exponential curve is incompatible with a turn-count floor.** Every v1 lost 0/100 — the ramp deck becomes the fastest deck. Structural to the shape, not the constants.
- **A curve change under ~20% is invisible to status cards**, because stacks are whole numbers. Status decks must be re-gated by hand after any curve move, and buffing the attack side is the finer instrument.

## Open items, in the order they should be taken

STRATEGIC (Henry, 2026-08-12): after balancing completes, decide 1v1-only vs 3v3 as the shipped mode — gates ticket 05, the team OSes (valkyrie L-family, einherjar_standard), and Steam scope. Henry's stated goal: ship on Steam. 1v1 balancing finishes first.

0-VALK-ENGINE. **DO NOT RE-DERIVE: valkyrie_v2's ENGINE IS HER OS, NOT HER DECK (ticket 60).** Zeroing REBIRTH_CYCLE's payoff costs her **50 of her 88 field points (87.7% -> 37.7%)**; **every single-card knockout lands inside +-3.3 on a +-5 instrument**, and four of five make her stronger. **Deck size is NOT a lever either** - 6 / 7 / 8 cards all read 87-91. Two consequences. (a) **Restoring her 8th card is FREE (+2.6) and must be paired with an OS change**, or the rulebook fix ships her at ~90%. (b) The two hypotheses that looked obvious were both wrong: the exhaust package does not drive reshuffles (shuffles/turn go UP when cards LEAVE - deck size drives cycling), and `starfall` is not a mispriced bomb but the deck's weakest card at **7.1 damage/cast, 3.11 casts/game**, whose removal is the study's biggest gain. **Her once-per-turn guard is load-bearing** - she reshuffles more than once on 34.7% of turns and the cap eats every one.
0-BURN-CEILING. **DO NOT RE-DERIVE: A THIRD OF ALL BURN PLAYED IN THE GAME IS THROWN AWAY, AND OVERFLOW PAYS ZERO (ticket 58).** Roster-wide **32.1% of applied stacks are wasted at the 3-stack cap, with 0 overflow damage across 54,767 requested stacks** - `BURN_OVERFLOW_PERCENT = 0.01` floors to zero on every frame under 100 max HP and no current frame reaches 100 (the code comment calls this intentional headroom). Per deck: **fenrir_v2 53.8%**, skoll_v2 19.7%, hraesvelgr_v2 9.4%, draugr_v2 0%. Worst card is **`molten_core` at 64% wasted**. **This bounds any "more Burn" design** - fenrir_v2 already applies 26,667 stacks to land 12,313. The DoT that DOES land is not small: **39% of fenrir_v2's damage (~25 HP/game) and 18% of skoll_v2's (~11.6)**.
0-FLOOR-LINE-NOISE. **THE FLOOR LIST'S 0.25 LINE IS NOT STABLE AT 50-100 ITERATIONS (ticket 58).** `fenrir_v1` appeared to "recover" 0.29 -> 0.194 and came off the list with **nothing touching it or the control**. Measured on the identical matchup: **0.205 at 150 iterations, and five seed bases at 50 iterations give 0.255 / 0.235 / 0.182 / 0.232 / 0.283 - a spread of 0.101** that swallows the entire move. **Decide floor-list membership at 150 iterations, or give the line a hysteresis band**; and pair this with 0-DECISION-GRADE, which found the same instability on the field axis.
0-ASSUMED-STACKS. **`ASSUMED_STATUS_COUNT = 3` IS A PRICE THE DECK MUST ACTUALLY SUPPLY, AND `ash_communion` IS THE CASE THAT PROVES IT.** It scores **10.6 against a 6.5 band - the registry's loudest card redline - and delivers 7.3 HP, about 1.5 stacks.** It consumes FENRIR'S OWN Burn, and two of his three Burn cards apply to the TARGET, so the deck cannot feed it. **It is also OUTBID rather than unaffordable** (in hand 281 turns: outbid 144, unaffordable 107, constraint-blocked 0). **Before treating a STATUS_CONSUMED redline as a card problem, check whether the deck supplies the stacks the scorer assumed.**
0-KRAKEN-WALL. **DO NOT RE-DERIVE: KRAKEN'S SUB-WINDOW NUMBER IS A WALL, NOT DECAY (ticket 65, 1,800 real battles).** kraken_v1 26.6% / v2 27.3%, but v1 **wins 75-100% vs four species and 0% vs EIGHT** (jormungandr, huldra, ymir, draugr, valkyrie, audhumbla, nidhoggr, ratatoskr - zero wins in 60 decided games each). Both decks share the shape, so it is SPECIES-level, not OS-level. **Damage rate is NOT the cause: kraken 13.0 dmg/turn vs valkyrie_v2's 12.9 - the same deck by that instrument, 18 field points apart.** **Do not spend a knob here**: TIDAL_CRUSH is worth 5.02 HP/game (8% of output) and `surge_protection`'s refund already fires 100% of casts, so every available dial moves damage output and damage output is not what separates her from a 45% deck. The indictment is the absence of an answer card for sustain/long games, the 58 HP frame (lowest in the roster) and chip cards delivering 1.4-5.6 damage a play. **Before designing, confirm none of the eight zeros is a mechanical LOCKOUT** - that would be a defect rather than balance and the diagnostic did not test for it. Pace is fine (5.2-turn mirror vs a 5.28 roster median).
  **DECOMPOSED 2026-08-16 (ticket 67 + Amendment 1)** ([research/kraken-lockout.md](research/kraken-lockout.md)). **NO STRUCTURAL LOCKOUT EXISTS** - 7 of 8 zeros dragged below 26% max HP, **5 of 8 taken to 0.0%**, v2 won against five of them in-sample; ymir is closest and still is not one. **The type confound was real and bigger than the diagnostic knew: her four wins are EXACTLY her four type-advantaged matchups, and her NEUTRAL-bucket win rate is 7.1/12.5% against the reported 26.6%.** But the deficit is **NET, not offense**: in neutral matchups **kraken deals 12.72 dmg/turn and valkyrie_v2 deals 12.51 - and valkyrie wins 64.4% to kraken's 10.7%**, because kraken takes 14.21/turn to valkyrie's 12.53 on a 12%-smaller frame. **Close -1.49/turn (~+12% rate or equivalent mitigation).** hel_v2 proves the game pays for RATE: worst damage taken in the roster, 75.5% win. **The pool cannot fix her** - no probe arm moved a zero off zero, and removing `capacitor` made v2 WORSE (it buys the 3-energy payoff turns). Sustain is NOT the common cause (jormungandr and nidhoggr sustain 0.00).

0-QUEUE. **DEEP-PHASE QUEUE (ticket 54): items 1 (jormungandr/55), 2 (Light polish/56) and 3 (hel_v2 Gateway/57) are DONE — 2026-08-13.** **The ticket-49 floor list is now EMPTY of decks the control beats**: hel_v2 was the last one at 0.810 control-wins and reads **0.000** after ticket 57. `kraken_v2` (queue item #5) and `fenrir_v1` came off earlier as a `surge_protection` side effect. **Use the field census for the middle of the roster, not the floor list** (0-CENSUS). Three decks now sit at the TOP end and want watching together: **valkyrie_v2 84.7%, jormungandr_v1 84.0%, hel_v2 78.2%** — the last two measured at 30 iterations. Next queue items: Fire (ticket 58, report-only) and the registry triage (59); ticket 60 is the valkyrie_v2 knockout study.
0-BURN-SHAPE. **DO NOT RE-DERIVE: DETONATION IS A TRADE OF DoT FOR BURST, NOT AN ADDITION (ticket 62's 21-arm grid).** Modulo-carry detonation SPENDS three stacks per payout, so the pile lives at the BOTTOM of the tier table instead of the top - fenrir_v2's tick damage falls 24.3 -> 19.8 HP/game under `DET-C3-D8`. That is why **every cap-3 DETONATE arm puts skoll_v2 BELOW her live baseline** (15.7-18.7 vs 27.0): her Burn is mostly tick and detonation eats the ticks. **Shape is worth ~44 field points** at a fixed cap and dial (VENT 78.3 vs DETONATE 34.6 at C3/D8) - more than the entire approved D range. Two other settled facts: **the waste is fixable at any D** (unpaid stacks 40.4% -> 0.0% in all 20 non-baseline arms, so ticket 58's 32% is a floor artifact, not a design constraint); and **BURN IS NOT skoll_v2's LEVER** - 15.1-39.0% across 21 arms, above baseline only in arms that simultaneously send fenrir_v2 to 66-82%. skoll_v2 is a card problem. **Symmetric self-burn is measured FREE** (<=0.95 HP/game to fenrir_v2 in the most expensive arm), so it is a rule for future card design rather than a live cost. `BURN_CONFIG` in `StatusBehaviors.ts` is the knob surface - arms mutate it in memory; the committed values are the live ones and `burnMechanic.test.ts` pins that.
  **UPDATED 2026-08-15 (Henry): the constant is now 1.5, not 3** ([research/assumed-stacks.md](research/assumed-stacks.md)). `ash_communion` is fixed - 9.3 -> 4.1, off the redline list - and section 1.3 fell 40 -> 37. **But the constant does duty for SEVEN scaling paths and 1.5 was measured from exactly one of them (consume / STATUS_CONSUMED, ticket 58).** At 3 it over-priced the paths reading a CONSUMED pile; at 1.5 it UNDER-prices the paths reading a BOARD pile, and by more, because those counts are bigger: DAZED_STACKS realistically ~10 (ratatoskr_v2), DISTINCT_STATUS 3 (draugr_v2), WEAKENED_STACKS ~6 (hexbloom). **`hexbloom` is the clean case - it scored 6.3 against a 6.5 band at ASSUMED=3, accidentally matching its own hand-price, and now scores 1.8.** `rimebreaker` left the redline list as collateral, not because anything about it was re-measured. **The error changed sign rather than shrinking. The real fix is to SPLIT the constant per path** - three of the four numbers needed are already written in comments in `powerscale.ts` - and that is a design call.
  **MEASURED AT LAST, 2026-08-15** ([research/status-pile-census.md](research/status-pile-census.md), 3,840 real battles). **The constant had never been measured and 3 is wrong for four of the six paths, in BOTH directions.** Consume **Poison 11.47** vs 3 (under by 3.8x; `wither_feast` 13.58 mean, `umbral_feast` max 79). **`DISTINCT_STATUS` 0.70** vs 3 (over by 4.3x, and this one is UNCONDITIONAL - `rimebreaker`'s redline is largely artificial). **`DAZED_STACKS` 3.62 - 3 is RIGHT and the "~10" comment in the file is measurably WRONG.** `WEAKENED_STACKS` 5.04, `BARKSHIELD_STACKS` 7.70. Board piles given one exists: BarkShield 7.70 / Sharp 7.61 / Poison 6.57 / Strengthened 5.90 / Weakened 5.04 / Dazed 3.62 / Burn 2.27 (capped) / Regen 2.25 / Asleep 2.01 / Energized 1.22. **Consume Burn 1.50, exactly ticket 58's estimate, with 22.7% of casts consuming NOTHING** - the cap plus 1/turn decay make Burn's pile unlike every other status's, which is precisely why 1.5 is Burn-only. **SHIPPED: `ASSUMED_STATUS_COUNT` is 3 again; `ASSUMED_CONSUMED_STACKS = { Burn: 1.5 }` carries the exception; a `STATUS_CONSUMED` heal resolves its status from its own card's consume action** (ash_communion/Burn and umbral_feast/Poison no longer share a number). §1.3 = **38**. **NOT changed: every other number the census indicts - those are design calls.** The structural one: **`MULTIPLY_STATUS` should read the pile of the status it doubles** (heat_wave/Burn 2.27 vs contagion/Poison 6.57).
  **REPRICED 2026-08-15 (ticket 66)** ([research/scorer-repricing.md](research/scorer-repricing.md)). Consumed: Burn **1.5** / Poison **8** / Strengthened **8**, fallback 3. Board: DISTINCT_STATUS **1**, WEAKENED **5**, BARKSHIELD **7**, DAZED **stays 3** (the one the census vindicated - the "~10" comment is DELETED). MULTIPLY_STATUS reads its own status's pile and prices doubling as a DIFFERENCE; repeated applications within a card ACCUMULATE. **§1.3 38 -> 42, §2-3 BYTE-IDENTICAL, and NOT ONE CARD CHANGED** - Henry's policy: the ledger got truthful, the cards did not. Watch `contagion` **20.4 vs a 6.5 band** (3x budget, the largest overage in the game) and `hexbloom` **16.5** - both Poison/Weakened payoff cards whose honest price nobody had seen before.

0-BURN-PRICE-LAG (**CLOSED 2026-08-15 — the scorer now DERIVES its Burn prices from the engine, so this cannot recur; kept for the lesson**). THE SCORER DID NOT KNOW BURN CHANGED. The engine ships DETONATE at cap 4 / 14% maxHP on a four-tier table; `powerscale.ts` still holds `BURN_TIER_POWER = [4.5, 15, 40]` (three tiers, cumulative at 3 power per 1% maxHP) and `BURN_OVERFLOW_POWER_PER_STACK = 3` - a number ticket 29 derived when an excess stack was worth 1% of a pool and floored to ZERO damage. **Every Burn card is therefore priced against a mechanic that no longer exists, and the 40 section-1.3 card budget redlines are measuring the wrong engine.** Do not read a Burn card's score as meaningful until this lands. The carry-forward arithmetic at the table's own rate is **`[4.5, 13.5, 28.5, 52.5]`** (cumulative 1.5/4.5/9.5/17.5% maxHP) and **42 power per DETONATION** - but note the second is a change of SHAPE, per-EVENT rather than per-excess-stack, because a card applying S stacks to a fresh target now causes `floor(S / cap)` detonations instead of `S - cap`. Held out of the ship deliberately: repricing moves section 1.3 and can indict cards, which is a design session. Working is in `power_curve_spec.md`'s ticket-62 rev note.
  **RESOLVED:** `BURN_TIER_POWER` now reads `DEFAULT_GAME_CONFIG.status.burnStacks` and `BURN_DETONATION_POWER` reads `BURN_CONFIG.overflowPercent`; `burnPower` mirrors `onApply`'s carry arithmetic; `BURN_OVERFLOW_POWER_PER_STACK` is DELETED (wrong SHAPE, not merely stale). `burnPricing.test.ts` pins the two by RUNNING `BurnBehavior` on a 10,000 HP frame and requiring the price to equal the damage delivered - a one-sided future edit fails there. **The general lesson: transcribing a corrected number fixes today and re-arms the trap; deriving it disarms the trap.**

0-BURN-NONMONOTONIC. **DO NOT RE-DERIVE: BURN IS NOT MONOTONIC IN STACKS APPLIED (ticket 62 repricing).** At the shipped cap of 4, applying FIVE stacks to a fresh target delivers **15.50% of max HP against four stacks' 17.50%** - the detonation consumes the pile that would otherwise have ticked down through the whole table. **Every multiple of the cap is a local maximum and the stack just past it is a local minimum** (8 -> 31.50%, 9 -> 29.50%). On an 80 HP frame 4 stacks deal 13 and 5 deal 12, so a card that pushes a target from 4 to 5 makes things BETTER for the target. `burnPower` in `powerscale.ts` is non-monotonic to match, deliberately - that is the engine, not a modelling artifact. Related and separate: **a card applying Burn in TWO actions is priced per action, not per pile** - `molten_core`'s 2+2 scores 27 where the engine builds one pile of 4 worth 52.5. That independence is old; the non-linear table is what made it matter.

0-SKOLL-CURVE. **skoll_v2 SHIPS WITH A RED DEAD-CARD GATE AND IT IS A CURVE PROBLEM, NOT A POWER ONE (ticket 64).** **36.9% / 38.2% on two seed bases against 0.35** - measured, not noise. She holds **three 2-cost cards on a 2-Energy frame in a 3.5-turn game**; a 2-cost card is her entire turn and the deck offers three of them. **Knob round 2 proved power dials cannot reach this number**: OS 15% -> 10% moved games only 3.50 -> 3.63 turns, made dead WORSE (37.6%), and cost 12.7 field + 20 control points. Both remaining authorized knobs are power dials. **The fix is one card swap - drop an `overdrive` copy for a 1e/0e Fire attack - which is outside the authorized list and therefore Henry's.** Predicted: dead ~33-34%, field down ~3-5 to the mid-40s, still in band. Do not spend another power knob here.
  **CLOSED by Amendment 1 (2026-08-15).** The one approved swap - `overdrive` x2 -> x1, `fury_strike` x1 -> x2 - took dead to **32.5 / 32.8%** on both seed bases. **The diagnosis held exactly**: removing the third 2-cost card fixed it, and `fury_strike` being the only 1e attack that FEEDS the OS meant the lost nuke partially returned as fuel. Cost ~6-8 field points and ~10-13 control points, both of which v2 had spare (field 44.9/41.5, control 71.7/74.6). **The lesson stands and is the reason to keep this entry: a dead-card gate is a CURVE reading, and power dials do not move it** - knob round 2's OS cut made it worse while costing 12.7 field points. Reach for the card list, not the dial.

0-FIRMWARE-CAP-ESCAPE. **A HOOK THAT NEEDS ITS OWN SCALING CAP CANNOT BE DATA (ticket 64).** `HookFactory.resolveScaling` hard-caps STRENGTH_STACKS at `STRENGTH_STACK_CAP` (8). SOLAR_OVERDRIVE_OS is specified at cap 5, so the identical data hook would have delivered **+120% at eight stacks where the design says +75% at five**. The data-model fix is a `scalingCap` field, which per 8c2 means zod AND the TS unions in two places each - for one consumer. **Hand-written firmware in `CustomFirmware.ts` is the precedent and the cheaper answer** (hel_v2's UNDERWORLD_GATEWAY, ymir_v2's GLACIAL_HEART). Related: **`STATUS_CONSUMED` existed on HEAL and STATUS but NOT on ATTACK** until this ticket - added power-side, so it rides the divisor/STAB/resistances rather than bypassing them (the ticket-26 lesson). **And the static scorer under-reads a consume-payoff card badly: `sun_devourer` scores 3.2 and measures 7.91 stacks consumed for 32.67 damage a cast** - a 2.6x gap. Never read a consume card's §1.3 row as its power.

0-CONSTRAINT-OVERRIDE. **AN INLINE `type` NEXT TO A CONSTRAINT `id` SILENTLY REPLACES THE LIBRARY DEFINITION (ticket 68).** `inflateConstraint` (programRegistry.ts:63) does `{ ...CONSTRAINTS_LIB[id], ...inline }` - **the inline object is spread LAST and wins.** `surge_protection` named `card_drawn_check` and then wrote `"type": "BASE"` beside it, so its draw condition was really an energy check against cost 0 and its refund fired on 3,371 of 3,371 casts. **Reference library constraints by `id` ALONE.** A regression guard now walks every card and fails if any inline `type` differs from the library entry it names (`triggeredDraw.test.ts`); the sweep found exactly one occurrence, so it starts green. Related and worth pairing: **action-level conditionals were not being passed `state`**, so every state-dependent one hit `ConditionValidator`'s `if (!state) return true` fail-safe - the same always-true-guard family as 0-TARGETLESS, now three occurrences.

0-BURN-CAP. **DO NOT RE-DERIVE: RAISING BURN'S CAP WEAKENS THE WHOLE STATUS, IT DOES NOT JUST MAKE OVERFLOW RARER (ticket 62 amendment 2's deep sweep, cap 3-8 x D 6-16).** C3 -> C8 at D16 takes detonations/game 3.27 -> 0.77 AND total Burn output 55.2 -> 24.7 HP/game, because amendment 1's spread tier tables lengthen the CLIMB as well as raising the ceiling - fenrir_v2's tick alone falls 23.3 -> 15.2 before any detonation is counted. By cap 7-8 the mechanic has largely stopped existing. **'Harder to reach without making Burn worse' is NOT available from the spread-tier construction**; it needs a cap raise with the climb held fixed, which is a new tier table and a design call. **Cap and D MULTIPLY, they do not add** - `C3-D10` / `C4-D14` / `C5-D16` are three routes to the same fenrir_v2 field number differing only in texture (2.96 / 2.00 / 1.57 detonations a game at ~7.7 / ~10.9 / ~12.4 HP each). **And the cap decides the collateral: at cap 3 detonation costs skoll_v2 ~6 points and pushes hraesvelgr_v2 over the ceiling; at cap 4 skoll is flat and hraesvelgr comes down** - so the scope concession amendment 2 spent is a CAP-3 cost, not a DETONATE cost. Cap also prices symmetric self-burn (1.2-1.4 HP/g at cap 3, ~0 at cap 5+). **FTK 0 across ~46,000 games at every cap and every D up to 16%**, largest single burst 14 HP - the burst-FTK worry is measured dead. Tick tables for caps 6-8 are GENERATED (agent construction, not Henry's) - see the report before building on them.

0-AI-SIM-COUNTS. **A HOOK WRAPPER COUNTS TacticalAI's LOOKAHEAD, NOT THE GAME (ticket 61).** `runOne` calls `getBestAction(state)`, and the AI evaluates candidate plays by pushing them through the reducer - so **every firmware hook fires ~24x per real proc**. Ticket 61's first sweep read **81-103 REBIRTH procs/game against ticket 60's 3.34** with nothing wrong with the hook. The failure mode is the dangerous kind: plausible numbers, not obviously broken ones, and the ratio is not constant across decks. **Any instrument that counts hook INVOCATIONS must exclude the AI's search** - raise a flag around `getBestAction` in a custom runner and count only unflagged invocations (reproduces ticket 60's number exactly). Second-order trap from the same ticket: **per-turn attribution is off by one boundary if you use the loop's own turn counter** - the player's start-of-turn draw, and therefore her reshuffle, resolves inside the reducer call that ENDS the opponent's turn, which put 77% of procs on the wrong side. Bucket by `ctx.state.turn`/`activeSide` read at the moment the hook fires.

0-DECISION-GRADE. **A 10-ITERATION FIELD READ IS NOT A GATE. Ticket 57 proves it cost a knob round.** hel_v2 measured **79.6% at 10 iterations — inside the 0.35-0.80 window — and 81.6% at 30 iterations, over the ceiling.** The knob was spent on the 30-iteration number. Mean sampling delta between the two reads across the roster is 1.92 points and the max is 5.4 (0-CENSUS), so **anything within ~5 points of a band edge must be re-read at 30 iterations before it is called a pass or a fail.**
0-DRAW-COUNTER. **`cardsDrawnThisTurn` COUNTS THE DRAW-PHASE DRAW, so any "if you drew a card this turn" condition is satisfied for every species on almost every turn (ticket 67).** Chain: `executeDraw` increments the counter and **`isNatural` is passed but never consulted for it** (`resolutionEngine.ts:531`); the draw phase calls the same function (`battleReducer.ts:943`). `surge_protection`'s refund therefore fired on 3,371 of 3,371 casts. **It is not literally inert though** - `cardsToDraw` is clamped by `HAND_SIZE_LIMIT - hand.length`, so a turn starting with a full hand draws nothing and the counter stays 0: measured at **8.8-9.6% of plays**. Treat this family as "conditionless ~91% of the time", and check the clamp before pricing any new card on a draw condition.
  **SUPERSEDED IN MECHANISM by ticket 68 - the counter was never the cause.** `surge_protection` carried an inline `"type": "BASE"` next to its constraint id, and **`inflateConstraint` spreads the inline object LAST**, so the library's draw check was overwritten by an energy check against cost 0 - always true. Separately the reducer never passed `state` to the validator, so it hit `if (!state) return true`. **Either alone made it unconditional; fixing one produced a 0-of-67-rows balance diff, and that null result is what exposed the other.** The natural-draw counter behaviour described above is real but was never reached. Both are fixed and `CARDS_DRAWN_TRIGGERED` now exists. **The scaling `CARDS_DRAWN` (ink_stream, starfall) still counts the refill and was deliberately left alone - those cards were balanced against it.**

0-COSTHOOK-BLOCK. **TO MAKE A CARD UNAVAILABLE, PRICE IT OUT — DO NOT ADD A THIRD CODE PATH.** Ticket 57's blood cap refuses a cast by returning an unaffordable cost from `onCostCalculated`. Both consumers already price cards through `executeCostCalculated` — the reducer's `handlePlayProgram` and `TacticalAI` (HANDOFF 8d) — so the AI never proposes the card, the reducer would reject it anyway, and the UI cost pip greys out for free. **A cancel-on-play or a constraint would have needed all three taught separately.**
0-CAP-TEXTURE. **A CAP THAT ALWAYS BINDS IS THE MIRROR IMAGE OF 8-INERT-CAP — AND hel_v2 MEASURED BOTH SIDES OF IT.** Her blood budget bound on **47.7% of turns at 20% and 83.0% at 15%**. **Henry shipped 20%, texture over the field number** (amendment 1), and the per-card table shows exactly what that bought and cost: at 20% the budget fits `soul_tithe` (15%) PLUS `venom_shade` (5%) exactly, so **`venom_shade` went 53.3% dead → 0.0% at 3x the plays** — but that same perfect fit crowds out `last_rites` (10%), whose dead rate rose **45.0% → 51.7%**. Deck-level dead cards still improved 15.1% → 12.2%. **Two lessons: report the at-cap RATE, not just whether the cap fired; and a budget that fits one exact pairing will starve whatever does not fit that pairing.** `last_rites` belongs to the dead-card cleanup item, not to a knob — hel_v2's knob rounds are both SPENT.
0-DECK-SIZE-EXCEPTION (**REVERSED by ticket 61, 2026-08-14 - `glimmer` is back, valkyrie_v2 is 8 cards, and the 8-12 band is whole again for every deck; the paragraph below is kept for the lesson in its last sentence**). valkyrie_v2 WAS a sanctioned 7-card deck (ticket 56 authorised "deck to 7"), against `baseDecks.test.ts`'s 8-12 template. Recorded as a NAMED exception in that test, not a widened band, so the next sub-8 deck still has to argue for itself. **Ticket 56 shipped this red** - it ran vitest before its deck change and not after; ticket 57 fixed it. **Re-run the unit suite AFTER the last content edit, not before it.**
0-STALE-FROM-NUMBERS. **CHECK A TICKET'S STATED "FROM" VALUE AGAINST THE LIVE ONE BEFORE SEQUENCING KNOB ROUNDS.** Ticket 56's Part 2 round 1 specified `REBIRTH_CYCLE_OS` payoff **15 → 10** — but 10 was already live, applied as ticket 53's knob round 1, so round 1 was a **no-op** and the sequencing ("round 2 ONLY if still >0.80") had no round-1 movement behind it. Harmless here because round 2's condition was objectively met on measurement, but a ticket authored against a stale value can silently spend a round on nothing.
0-HEALPOWER. **DO NOT RE-DERIVE: A CARD HEAL'S PRINTED POWER USED TO BE DISCARDED BEFORE ANY HOOK COULD SEE IT.** `HealExecutor` resolves `calculateHeal` itself and emits an `HP` mutation with `isHeal: true`, so `handleHealEffect` received it as a `flatHeal` — an HP number. **That, not a design choice, is why NOURISH_ROUTINE could only be denominated in HP**, and why it converted ~4.5x smaller than its card text implied on an 86-HP frame. Ticket 56 carries `healPower` through the mutation into `last_heal_power`, read by the `HEAL_POWER` scaling. **Any future effect that wants to read "the number printed on the heal" uses `HEAL_POWER`; `HEAL_INTENDED` reads HP and now has no consumer.**
0-TARGETLESS. **DO NOT RE-DERIVE: A HOOK ACTION CAN BE SILENTLY DROPPED IN TWO WAYS, AND BOTH HAVE COST US A WHOLE OS.** (1) **Missing `"target"`** — `HookFactory.executeActions` early-`continue`s any non-`LOG` action whose target resolves to null, so a `COUNTER`/`DRAW`/`MAX_ENERGY` with no `target` field never runs. (2) **zod strips unknown keys** — a field added to `HookTypes` but not `HookSchema` vanishes between JSON and engine (8c2). THREE occurrences: GENESIS_FIRMWARE (ticket 53), OUROBOROS_LOOP (ticket 55 — dead since ticket 16, eight tickets), and `jorm_v1_reset`'s missing `when` (same ticket, a third shape: a hook that fires for BOTH sides because it never scoped its source). **When a hook "does not work", check the action actually ran before assuming the guard is wrong.**
0-LIVENESS-SWEEP. **THE SWEEP EXISTS — `liveness.ts`, ticket 55 amendment 1.** Two passes: STATIC replicates HookFactory's guards over every hook in `hooks.json` (dropped targets, zod-stripped keys, empty `do`, no-op modifier hooks); DYNAMIC wraps every registered hook fn and runs ~450 probe battles per OS, recording invocations and observable effects. **At `1:53ea4a83` it reports ZERO static findings and all 32 OSes LIVE.** Re-run it after any `hooks.json` edit — it is cheap and it is the only instrument that can see a dead hook. **A dead hook is indistinguishable from a weak deck on every other instrument we have** (jormungandr_v1 was ticket 49's top-priority "genuinely real" floor entry; the OS fix alone took its control matchup 45.0% → 96.7% on the unchanged deck).
0-HULDRA-BUFF-LEVER. **`huldra_v2_bark_start` IS DELETED (ticket 55 review) — and the deletion left a known buff lever behind.** It measured 0 effects across 10,649 calls while its twin `bark_end` took 100% of the grants, because a battle opens mid-turn-1 in the ACTION phase so **every unit's first boundary is a turn END, on both sides** (ticket 07's claim about enemy-side Huldra is measurably false). **Moving the surviving hook from `onTurnEnd` to `onTurnStart` is NOT a neutral restoration of intent — per 8-SHIELD-TIMING it is a real BUFF**, because a start-of-turn shield protects the owner's own actions. huldra_v2 sits at a healthy ~47-56% field and does not need it. **If it ever does, that one-word change is the cheapest lever on the deck.**
0-CENSUS. **THE ROSTER CENSUS EXISTS — [research/roster-census.md](research/roster-census.md), read at `1:53ea4a83`.** Three tables the roster had never had: (a) **first-mover edge per species** — range **−33.0% (hraesvelgr) to +24.5% (skoll)**, and **the extremes cluster on the three SHORTEST mirrors**; jormungandr's −13.0% is fifth in magnitude, so it is not the outlier the ticket-55 report implied. No threshold exists for this metric; **|edge| > 20% is offered as a candidate pace defect** and flags exactly hraesvelgr and skoll. (b) **Field census at 30 iterations (~900 decided games/deck)** — **17 of 32 decks inside 0.35-0.80, not the 13 a 10-iteration read suggested.** Mean |delta| between the two reads is **1.92 points, max 5.4**, so treat 10-iteration field numbers as ±5 and never tune against them. (c) **TOXIN_FANG A/B.** **Do not read a bimodal field as a defect one item into a six-item queue** — and eleven of thirteen bottom decks beating the control is the control discriminating the FLOOR, not the middle, which is its job.
0-TOXIN-LENGTH. **DO NOT RE-DERIVE: `TOXIN_FANG`'s `bonus` sets GAME LENGTH, and game length is what decides whether a hold-and-double card is castable.** The hypothesis on record was the opposite (raise the bonus, make the pile worth doubling). Measured at 1/2/3: field **61.3 / 83.3 / 88.7%**, v2 game length **4.18 / 3.71 / 3.34** turns, `contagion` dead **0.439 / 0.619 / 0.731** with **157 / 96 / 67** casts. **`bonus: 1` SHIPPED as knob round 1 (2026-08-13)** and reproduced every predicted number exactly. The general form is worth carrying to other decks: **a card that wants to be HELD is priced by the clock, not by the payoff** — if it reads dead, check how long the deck's games last before touching its numbers.
0-DEAD-HOOK-FLOOR. **A DEAD HOOK LOOKS EXACTLY LIKE A WEAK DECK ON EVERY INSTRUMENT WE HAVE.** jormungandr_v1 was ticket 49's top-priority "genuinely real" floor entry at 0.71 against the control. Measured in ticket 55: **the OS fix alone, on the UNCHANGED old deck, takes it to 96.7%.** Before tuning any deck on that floor list, confirm its firmware fires.
0-JORM-OVERSHOOT. **jormungandr v1 83.0% / v2 61.3% field** (v2 after knob round 1, 2026-08-13). **v1 is the only one still out** — 3–4 points over the 0.35–0.80 ceiling (84.0 on the 30-iteration census), **ACCEPT-AND-WATCH, no lever authorised**; the re-read happens after ticket 56 moves valkyrie_v2/audhumbla_v2, and it now reads the **POST-knob** field. Everything else is in: **§2.3 went 0.910 → 0.630 on the OUROBOROS cap and reads 0.940 after the v2 knob — DIAGNOSTIC ONLY, do not tune it** (deep-phase policy; v2 got deliberately weaker, so the gap widened by design). Mirror 3.2 turns at 400/400, first-mover −16.8%, max plays/side-turn 7, FTK 0. **The cap that fixed the degeneracy lives in FIRMWARE, not the deck list** — players build from the shared pool, so a curated list can never contain a chain; only a firmware cap is pool-proof. Still open: `contagion` at **0.439** dead against a 0.35 bar — **known and accepted, it belongs to the dead-card cleanup queue item, and jormungandr's knob round 2 is BANKED.**

1. **TYPE ADVANTAGE IS 1.5x WITH NO RESISTANCE (ticket 35) — and resisted pairs are ABSENT from the matrix, never written as `1.0`.** `getModifierBreakdown` multiplies any *defined* secondary-element entry by `SECONDARY_MITIGATION` (0.75), so an explicit `1.0` becomes a silent **25% penalty** on a matchup meant to be neutral. Absence means "no interaction". Consequence: `effectiveness` is never below 1, so the **"Not very effective…" log is unreachable** — it is left in place deliberately and a test pins that.
2. **Do NOT shave the type multiplier again — the data says that path is a dead end.** Measured at 1,440 games per variant: a **persistent multiplicative** damage modifier is a win condition, not matchup flavour. Even **1.05/1.0 produced an 89/11 cross-element split**, and pace is not the amplifier either (8.6-turn games measured the same spread as 4.4-turn ones, marginally *worse* — more turns means less variance, so a persistent edge converts more reliably). If cross-element ever needs to be competitive, change the mechanism's **shape**: non-persistent (first hit each turn / once per battle), **additive** (+N flat, self-limiting and non-compounding), paying out in energy or draw instead of damage, or rubber-banding the resisted side.
3. **Same-species gates are blind to the type matrix.** §2.3 and the mirror both pit a species against itself, so no elemental modifier applies — a type change cannot move them, and the registry hash does not change. The only cross-element suite is the archetype gauntlet. When judging a type change, measure the gauntlet or a round robin, never §2.3.
4. **Regen's stacks are DURATION, not intensity (ticket 34) — do not re-derive this.** A flat 3% of maxHP per turn, lasting `stacks` turns. The engine used to multiply by stacks, making one application worth `1.5·N(N+1)` percent of a pool, and with a flat 1/turn decay it was **the only uncapped heal in the game** — 15 stacks healed 45% of a pool EVERY TURN. **Poison looks identical and is NOT**: Poison's stacks are intensity (`1.5·S(S+1)` is correct for it), Regen's are duration (`12·stacks`). Three places model this — engine, AI eval, powerscale — keep them in lockstep.
5. **Diagnostic lesson from tickets 33/34: when ONE card decides a matchup, isolate every clause on it before naming a cause.** huldra_v1 was blamed on ALLURE_PROXY (measured 3.5 Weakened, a bit player), then on Sharp (cutting it 3→2→1 moved §2.3 by 0.02). It was the Regen riding along on the same card — every `iron_bark` variant without it measured **0.000** regardless of buff type or magnitude.
6. **huldra_v1 is still a one-card deck.** Even after the fix, `iron_bark` ×1 measures 0.030 and ×0 measures 0.000. Its damage is two `thorn_tithe` and one `hexbloom`; it needs a real payoff, which is a design call and not a knob.
7. **`overgrowth` (3 Regen, 1e) is in no deck and scores 3.60 vs a 3.0 band** — re-check before it enters one. It was a corrected 7.20 before ticket 34.
8. **Next species: nidhoggr** — he finishes Dark, and he is now the most broken thing left: **396/400 draws at 60.7 turns**, unchanged since baseline. He shares the Dark pool with Hel, so `venom_shade` (1.8) and `curse_mark` (1.3) — both well under band, both in his placeholder deck — are his to fix, not Hel's.
8a. **THE POISON EVAL IS HORIZON-CAPPED (ticket 40) — do not re-derive this.** `S(S+1)/2` is the right total for DECAYING poison and only if the battle lasts S more turns; at 18 stacks it priced a pile at **171% of a health bar**. Now `min(S(S+1)/2, S x STATUS_HORIZON_TURNS)`. It matters most under nidhoggr_v1's ROOT_CORRUPTION, where poison does not decay at all, so the triangular shape is simply WRONG and the AI would never cash a detonate. **Cashing beats holding only when the horizon is below 3**, so there is no gentler setting available. Cost: **huldra 0.660 -> 0.030** (her old number was propped up by the bug — she is the one-card deck this file has flagged since ticket 34), ratatoskr 0.610 -> 0.330, jormungandr 0.390 -> 0.310. Everything else byte-identical.
8-DIFF. **DIFF THE MATCHUP TABLE, NOT JUST THE REDLINE SET.** A redline that was ALREADY lit hides any amount of movement underneath it: ticket 43 dropped os:ratatoskr 0.330 -> 0.180 and the review missed it entirely, because `OS_GAP os:ratatoskr` appeared in both the before and after lists so the set diff was empty. Compare decisiveWinRate/turns/deadCards per matchup every time.
8-TERMINAL. **A LOSS IS TERMINAL IN THE EVAL NOW (ticket 44), not -50.** `evaluateState` returns +-10000 when either side is wiped, so a win dominates any board and a loss is worse than any board. A MUTUAL kill lands at 0, between them - which is what a draw is worth. The per-unit +-50 from ticket 38 still governs multi-unit parties. **Two species fell out of band on the correctness fixes and need passes: os:jormungandr 0.240 and os:ratatoskr 0.200.** valkyrie also collapsed (control 0.53 -> 1.00) on ticket 43's healing_light conversion - her deck pass starts weaker than any pre-43 number suggests.
8-BURN. **Burn's eval is horizon-capped too (ticket 44).** Same fix as Poison: the decay sum is the right total only if the battle lasts `stacks` turns. At 8%/turn top tier, 10 stacks read as 69% of a health bar and 15 as 109%. Below ~3 stacks nothing changed.
8-HEAL. **`healOverride` NO LONGER EXISTS IN CARD DATA (ticket 43).** Every card heal is power-based so it scales with level; a flat heal was overpowered early and negligible late. The field is gone from `HealActionData` AND `HookSchema`. The engine-internal flat path is called `flatHeal` and is reachable only from `applyMutations` (hook/percentMaxHP heals) - do not reintroduce it on a card.
8-HEAL-b. **The scorer now prices `consume` and `STATUS_CONSUMED` heals correctly.** A consume used to score as an APPLY (eating an enemy's Poison ADDED to the card), and `STATUS_CONSUMED` never reached the HEAL branch. Both fixed; `soothe`'s negative-stacks sign bug (item 14) is a DIFFERENT path and is still open.
8-HEAL-c. **`ash_communion` scores 9.70 against a 6.5 band and is in fenrir_v2.** Newly visible, not newly strong - the card never changed. Wants a fenrir polish pass.
8-FLOOR. **THE CONTROL IS THE FLOOR, NOT THE MEDIAN (ticket 45).** Re-targeted to ~25% against the field (measured 0.237) on a 105/100/95 frame: it should be the WORST deck in the game, so beating it is a low bar and the reading is by how much. It carries a **2e cleanse** (`baseline_purge`) because without one it had no answer to a status clock and over-reported every DoT deck. **DEFENSE dominates its frame** - 90 -> 95 is worth ~14 points, HP 105 -> 110 about 2. Current floor test: jormungandr 0.71, draugr 0.86, valkyrie 0.98 all LOSE to it.
8-FLOOR-b. **A maintenance effect is worthless on top of an application rate that outruns decay.** ROOT_CORRUPTION maintains nidhoggr's poison, but he applies 10 stacks by end of turn one against 1/turn decay, so capping the maintained pile measured as a literal no-op. Check the application rate before tuning any 'does not decay' clause.
8-FLOOR-c. **nidhoggr resisted FOUR levers** - knob reverts, the pile cap, a 35% poison cut, and a frame down to 75/80/70 (below every species). Its strength is the SHAPE: many cheap applications compounding into a permanent rate. Target application volume per turn or the card count, not individual card numbers.
8-CLEANSE-SWITCH. **A FULL CLEANSE IS A SWITCH, NOT A DIAL — measured three times now, stop re-deriving it.** Ticket 45 priced the control’s at 2 Energy on this suspicion; ticket 47 measured `purify` swinging ratatoskr 0.21 → 0.65 with nothing in between; ticket 48 measured `deathless_slumber`’s cleanse worth **36 points of §2.3 and 20 of field on its own**, while the ticket’s own first knob moved the gate by zero. **A partial shed IS a dial** and prices sanely under ticket 47’s removal cap. Print a full CLEANSE only when a hard counter is the intent.
8-SHARED-CURRENCY. **TWO DECKS CAN SHARE A DECK LIST FIX AND STILL SHARE A CURRENCY (ticket 52).** gullinbursti_v1 and v2 got different cards, but five of v1’s ten still GENERATED Sharp while **the scaler that cashes Sharp lived in v2’s firmware** — v1 carried a mean 9.2 Sharp with no payoff and dealt ~27 damage a game against v2’s ~81. **§2.3 read 0.000 and TWO authorised knob rounds moved it by zero games** (halving the winner’s OS, raising the loser’s payoff 50%). Giving v1’s prime a Sharp scaler moved it **0.000 → 0.780 in one change.** When a gate will not move at all, ask what RESOURCE each deck generates and which one can spend it — before touching any number.
8-SHARED-CURRENCY-b. **A drawback that consumes the OS’s own resource is the cleanest nerf shape.** KINETIC_RAM now takes 1 self-Dazed a turn, and **Dazed is Sharp’s duality partner**, so the cost is spent cancelling the exact stat the OS scales on. Self-limiting by construction. At 2 stacks it overshot to 0.950; at 1 it lands. Worth reaching for before another flat percentage.
8-ENERGY-TRAP-3. **THIRD OCCURRENCE: `processPreTurn` SETS `currentEnergy = maxEnergy + Energized` rather than adding it.** Any firmware that grants raw ENERGY outside its owner’s own turn is granting nothing. BLOOD_SCENT (39), PERMAFROST_WAKE (48) and now CORRUPTED_GOLD (52) all shipped with this bug. **Pay in `Energized`, or pay at `onTurnStart`.**
8-COMPOUND. **Firmware damage bonuses and status percentages COMPOUND, they do not add.** `onDamageCalculated` hooks run at step 3 of `applyDamageModifiers`, status percentages at step 4, so a flat hook bonus is multiplied by Dazed/Weakened afterwards: `(base + bonus) × (1 + pct)`. Ticket 52 measured this on `crag_barrage`, whose own Dazed rider therefore multiplies the NEXT multi-hit’s firmware bonus too.
8-SHED-BAND. **A shed card cannot reach a 1e band, and Earth proved it on a real card.** `slag_shed` scores **1.70 against a 2.4 floor**: ticket 47 caps a card’s total self-facing removal at `CLEANSE_POWER`, so the removal half prices at 1.00 where uncapped it would be 2.20. **This is ticket 51 §5’s open pricing question, now with evidence.** Do not "fix" such a card upward without resolving that first.
8-INERT-CAP. **`maxCardsPerTurn` IS INERT WHENEVER IT EQUALS THE ENERGY CAP (ticket 50).** At 2 Energy with no 0-cost cards the most a unit can play is already 2, so GLACIAL_PACE’s "drawback" cost nothing and its +35% damage was paid for by nothing — v2 won **97% of 96 decided games** against v1 on an identical deck. Now +25%, priced as the pure bonus it is; the cap stays as a guard against a future 0e-heavy build. **Generalises: a card-count cap is only a cost if the deck can actually play more cards than it.**
8-SHIELD-TIMING. **A shield granted at END of turn is worth nothing to a shield-payoff deck** — it is eaten before the owner acts, and the payoff card reads zero. GLACIER_HEART grants at `onTurnStart` for exactly this reason. **And BarkShield stacks are FRACTIONAL** (`shieldPercent − absorbedPercent`, then ×0.8 decay), routinely 7.36 — any scaler reading them must `Math.floor`, or it reproduces ticket 36’s fractional-product bug.
8-SHATTER. **`shatter` IS STRUCTURALLY DEAD AND NO RE-COST FIXES IT (ticket 50).** `StunnedBehavior.endTurn` always returns `null` and the end-of-turn loop processes the ACTIVE side, so a Stun applied on turn T is gone before the applier’s turn T+1. Any "+damage if the target is Stunned" payoff must be cast in the SAME turn as the stun — 2e + 1e = 3 Energy after ticket 48, which no Ice species has. Left in the registry as a drop-only card. **The fix, if it is ever wanted, is a 0-cost payoff, not a re-cost.**
8-DUALITY. **`DUALITY_MAP` is a targeted cleanse that costs nothing (ticket 50).** `handleApplyStatus` cancels Strengthened↔Weakened and Sharp↔Dazed stack-for-stack BEFORE the behaviour runs, so a card granting 3 Strengthened + 3 Sharp sheds up to 3 Weakened and 3 Dazed and banks any surplus as the buff — never a dead draw. **Strictly better than a CLEANSE for that job**, and relevant to ticket 51.
8-CLEANSE-GONE. **CLEANSE IS RETIRED FROM CARDS (ticket 51, DONE).** Henry’s call: all card-facing removal becomes "shed N stacks of a named group", and CLEANSE survives only as an unused engine primitive. **Do not print a new CLEANSE card.** Groups: Weakened+Dazed (exists: `soothe`, `shrug_off`) and **Poison+Burn (new)**; Stunned/Asleep deliberately NOT sheddable.
8-CLEANSE-GONE-b. **Measured over 7,686 side-turns across all 22 decks and BOTH firmware slots:** a unit carries a DoT on **40.4%** of turns (Weakened/Dazed 45.2%), and **Burn alone on only 12%** — which is why the DoT shed is grouped. **Burn NEVER exceeds 3 stacks in the whole sample** while Poison reaches 60, so the two need different shed sizes or an explicitly asymmetric card. Least-afflicted deck (huldra_v1) still carries a DoT 31% of turns; most (nidhoggr_v2) 63%. **A first pass of this measurement using `matchupScenario` defaults saw ZERO Burn** — the default is `availableOS[0]` and every Burn deck is a slot-2 deck. Same trap as the gauntlet’s (8-GAUNTLET). Always iterate `availableOS`.
8-CLEANSE-GONE-c. **REMOVAL COSTS MORE THAN APPLICATION — `REMOVAL_PREMIUM = 1.25` (ticket 51).** Henry’s rule, and it is an argument about the game not the sampler: **an answer cheaper than the threat it answers makes the status archetype structurally dead.** It replaced ticket 47’s cap, which was flat — shedding 2/2, 3/3, 4/4 and 5/5 all scored exactly 1.00, so the scorer could not see shed SIZE at all. Applies to every self-facing removal, negative stacks and self-`consume` alike. At 1.5 `purify` breaches; 1.25 is where the roster lands.
8-CLEANSE-GONE-d. **THE CONTROL’S CLEANSE WAS CARRYING ALMOST THE ENTIRE FLOOR READING.** Swapping it for a DoT shed moved **jormungandr_v2 0.99 → 0.04** and **ratatoskr_v2 0.50 → 0.02** while 26 of 32 decks moved by ±0.01; slot-2 aggregate 0.326 → 0.219, slot-1 barely moved. **Ticket 49’s table and its priority order are STALE** — jormungandr_v2 was never a 0.99 deck. Re-run before tuning anything against a control number.
8-SHED-GROUP. **A shed is only worth what its opponent actually applies.** Pointing `deathless_slumber` at the DoT group took `os:draugr` to 0.220 because draugr_v2 applies Weakened and Dazed; re-pointing it at Weakened/Dazed restored 0.340. Pick the group from the matchup, not from the flavour.
8-CLEANSE-SWITCH-b. **A cleanse on a deck that statuses cannot interrupt is worse still.** Draugr sleeps through DoT, so his cleanse ran on a clock the opponent could not touch — and the decks he beat were exactly the status decks (huldra 1.00, hel 0.98, nidhoggr 0.96, ratatoskr 0.84). **Henry’s open question about the CONTROL’s cleanse doing the same thing is ticket 49 §4 and is still unmeasured.**
8-ASLEEP. **ASLEEP IS A STANCE NOW (ticket 48).** It loses 1 stack per INCOMING ATTACK (absorbed hits included) as well as 1/turn, statuses and DoT never wake the sleeper (the `sourceId === 'SYSTEM'` guard in `handleAttack`), and BOTH wake paths now grant a turn of StableOS — the damage path never did, though `statusGlossary` always claimed it. An OS opting in with `actsWhileAsleep` waives the printed `not_asleep` constraint **for its owner only**; the constraint stays on all 182 cards so a drafted card cannot become castable-while-asleep for anyone else. **The AI eval had to learn this too** — it prices 3 Asleep at −60% of a health pool, so without the `actsWhileAsleep` early-return the deck measures unplayable for a reason that looks nothing like balance.
8-ASLEEP-b. **`handleExecuteIntent` still blocks a sleeping unit**, so a Draugr on MOVES/intents will NOT act in its sleep. Known asymmetry, deliberately unfixed — the balance suite runs CARDS on both sides.
8-ASLEEP-c. **The 0.5 Asleep-conditional discount is measurably wrong and deliberately unchanged.** Draugr is asleep **65.1%** of his turns, not the ~50% it assumes, so `nightmare` and `barrow_rot` are under-charged as printed. Re-pricing them moved the gate by zero, so Henry left it — price the NEXT Asleep card against 0.65.
8-ASLEEP-d. **`grave_rest` scores 0.00 against a 0.8 floor and is the most-played card in its deck** (6.5 plays/game). `ASLEEP_SELF_POWER` prices self-sleep at a flat −1.1 and cannot see the stance’s payoff. **Do not buff it back.** Same blind spot the constant’s own comment warns about, from the other direction.
8-FLOOR-MOVES. **THE CONTROL AGGREGATE IS MEASURED AGAINST A MOVING ROSTER.** Ticket 48 dropped slot-1 from **0.235 to 0.185** without touching the control, purely because draugr stopped being a free win for it. Ticket 45’s 0.237 calibration is not wrong, it is DATED — re-read the aggregate after every deck pass and do not treat a drift as evidence the control changed.
8-STALEMATE-FIXTURE. **`runBatch.test.ts` needs a species whose mirror never ends** (`STALEMATE_SPECIES`, now `audhumbla`). It was `draugr` until ticket 48 gave it decks that finish in 6.7 turns. When those three tests go red, the deck got better — repoint the fixture, do not "fix" the runner.
8-COUNTER. **§2.3 CAN REPORT A COUNTER RATHER THAN A GAP (ticket 47) — check the field before you pick a direction.** ratatoskr sat at 0.200 head-to-head while the two decks measured **0.469 and 0.416 against the field**, with near-identical per-opponent profiles. Nerfing v2 is what §2.3 literally asks for and would have pushed the WEAKER of the two further down. **A round robin of both decks against the field is now the first move on any OS_GAP redline, not the last.**
8-COUNTER-b. **The two ratatoskr firmwares pay out at the same RATE and wildly different value.** GOSSIP_NODE gives 2.5% of maxHP per 0-cost play — **1 HP** on a 62 frame, ~17 a game; INSTIGATOR_OS gives 1 Dazed per 0-cost play — 17.4 a game, cashed by `slander` at a median 13 stacks for 16.2 damage. When two firmwares trigger on the same event, compare what they PAY, not how often they fire.
8-COUNTER-c. **A full cleanse is a switch; a partial shed is a dial.** `purify` swings that matchup 0.21 → 0.65 with nothing in between; −1/−1, −2, −2/−2 and −3/−3 read 0.30 / 0.37 / 0.41 / 0.60. Ticket 35’s law holds: **when a mechanic is a coin flip, change its SHAPE, not its number.**
8-COUNTER-d. **In a 0-cost spam deck, utility at 1e is strictly worse than the same utility at 0e** — a LARGER 1e shed measured 0.388 against the 0e version’s 0.407. It costs the whole turn’s Energy and skips both the `echo_chamber` token and the OS proc. A 0-cost deck pays for utility twice.
8-COUNTER-e. **The symmetric enabler fix OVERSHOT.** GOSSIP_NODE shedding 1 debuff stack per 0-cost play — the exact mirror of INSTIGATOR_OS — lands at **0.807**, because v1 plays ~34 cards against v2’s 17 Dazed and the rate cancels the engine. Enabler-first is right about where to LOOK; the rate an enabler fires at can make it unusable as a knob.
8-GAUNTLET. **THE GAUNTLET NOW RUNS EVERY FIRMWARE (ticket 47).** It used to take `availableOS[0]`, so **half the roster had never been measured against the floor**, and §2.3 could not cover for it — it reports the gap BETWEEN two firmwares and is blind to where either sits, so a deck could be under the floor with every gate green. Slot-1 aggregate **0.235** (ticket 45 calibrated to 0.237 against exactly this set — the floor is intact), slot-2 aggregate **0.367**, pooled 0.301. Rows are keyed `gauntlet:control-vs-<species>:<os>`; the per-slot aggregates have their own rows so the calibration stays checkable.
8-GAUNTLET-b. **EIGHT OF SIXTEEN SLOT-2 DECKS ARE AT OR BELOW THE FLOOR.** `jormungandr_v2` 0.99, `valkyrie_v2` 0.95, **`hel_v2` 0.94**, `draugr_v2` 0.77, `skoll_v2` 0.64, `kraken_v2` 0.53, `ratatoskr_v2` 0.50, `fenrir_v2` 0.44 (the control’s win rate). **hel is the loud one** — she shipped in ticket 36 passing every first-pass band, and every one of those bands was blind to this. Each wants its own pass.
8-GAUNTLET-c. **Gauntlet rows changed IDs and seeds in ticket 47** — `matchupScenario`’s default seed embeds `enemyOS`, now always explicit. Slot-1 rows moved within ±0.07 with no structural change; every `os:` and `mirror:` row outside ratatoskr stayed byte-identical, which is the check that proves the movement is reseeding. Do not diff a pre-47 gauntlet row against a post-47 one.
8-REMOVAL. **A PARTIAL REMOVAL CANNOT PRICE ABOVE A FULL CLEANSE (ticket 47).** Once ticket 46 priced CLEANSE from measured LOAD (10 power) while the status tables kept pricing STACKS, "remove 2 Dazed + 2 Weakened" scored **1.70** against a full cleanse’s 1.00. A card’s TOTAL self-facing debuff removal is now capped at `CLEANSE_POWER` — on the card, not per action, because the dominance argument is about what the card does. Four cards touched, all downward: `soothe` −0.80 → 0.80, `shrug_off` 1.00 (1.50 uncapped), `umbral_feast` 2.60 → **2.00 (now under band — a reading, not a defect)**, `ash_communion` 9.70 → 7.10.
8-CLEANSE. **CLEANSE IS PRICED AT 10 POWER (ticket 46) — measured, and deliberately low.** Sampling the debuff load on the acting unit at every side-turn across all 90 pairings of the ten tuned species (4,922 samples, 540 games) put the honest central estimate at **13–17 power**: 63.3% of turns carry at least one debuff and the median when loaded is 15.0, trimmed mean 13.4–16.9. **Shipped at 10** because the sampler conditions on being loaded and the card does not — it is dead on the other 36.7% of turns, and it has to be in hand at the moment it is worth something. **Do not read the raw mean (51.8) or the max (6678.5)**: `powerscale`’s `poisonPower` is still the UNCAPPED triangular sum — ticket 40 capped the AI eval only — so nidhoggr’s piles dominate the tail. **That divergence is provably a no-op today** (the cap does not bind below 5 stacks and no card applies 5+ in one action) and was left alone; it goes live the moment a card does. `purify` 0.90 and `baseline_purge` 3.90, both still under their bands, which is the intended direction of the lowball. **A bare 1e cleanse with no rider is a weak card on this pricing** — a cleanse wants to be stapled to something.
8-CLEANSE-b. **CLEANSE is out of `MANUAL_REVIEW_TYPES`. SEARCH, PLAY_LAST_CARD and TRIGGER_STATUS still score a silent 0; MULTIPLY_STATUS scores a heuristic off `ASSUMED_STATUS_COUNT` and flags for review anyway — an estimate, not a measurement.** The same harness shape — simulate the roster, sample the thing the action would act on, price it in powerscale’s own units, then lowball — prices the rest of them.
8-CONTROL. **THERE IS A CONTROL DECK NOW (ticket 42) — measure absolute power against it, not §2.3.** §2.3 is same-species and reports a GAP; it cannot see where both decks sit. nidhoggr passed its §2.3 at 0.310 while sitting at **87-94% against the field**. The `control` species is None-element (inert both ways), firmware-free, cards exactly at band, frame 110/105/95, and it appears in every run's report as the gauntlet benchmark. Low control win rate = that species is strong. Current spread: **0% (nidhoggr, huldra, ymir, audhumbla) to 91% (jormungandr)**. **DO NOT TUNE THE CONTROL** - its whole value is that it never moves.
8-CONTROL-b. **On-curve is NOT competitive.** On the median species frame the control lost **97.9% of 1600 games**. A deck's power is cards + firmware + synergy; the curve prices only cards, and the other two are worth ~+37% HP / +24% attack. Never read a powerscale score as a deck's strength.
8-CONTROL-c. **FIXED in ticket 43** - a run missing any expected suite now prints what is missing and leaves `docs/balance/` untouched, so a single-suite run can no longer corrupt the committed report.
8a0. **A CARD THAT CONSUMES A STATUS CAN EAT THE OPPONENT'S WIN CONDITION (ticket 39).** nidhoggr_v2's `umbral_feast` consumes its OWN Poison - which in the §2.3 gate is mostly the ~19 stacks v1 applied, cashed for ~33 HP a cast. The deck was converting its own clock into the enemy's sustain. Fix was to trade Poison AWAY for raw power on the applier (`blight_bloom` 35/4 -> 50/2): direct damage is the one thing a consume-heal cannot convert. Check this whenever both sides of a matchup touch the same status.
8a0b. **Knob ranges written before ticket 40 are stale.** `wither_feast` at `count: 3` fired 7 times in 100 games because cashing beat holding by only `3S - 2.5S` under the new horizon cap; count 5 was needed. Any pre-40 detonate maths needs re-deriving against `STATUS_HORIZON_TURNS`.
8a1. **A CONSUME card has no budget ceiling; a READ card does (ticket 41).** hexbloom consuming the Weakened pile made its value scale with how long you saved up - x3 measured **13.90 against a 6.5 band**. Reading the pile without spending it (`WEAKENED_STACKS`) turns the card into a RATE capped by the pile size, so x2 is enough and scores 6.30. Any future 'cash in a hoard' card has this problem by construction.
8a1b. **huldra_v1's damage is capped by her own enabler.** `thorn_tithe` self-Weakens her 3 to feed the ALLURE_PROXY mirror, so she swings at the -25% floor, and huldra_v2 is a BarkShield+Sharp mitigation deck. Flat power does not work on her - a 65-power card measured 8.2 damage. Poison (%maxHP DoT) ignores both sides of that.
8a2. **CLOSED in ticket 44** — Burn did carry Poison’s uncapped shape; `burnTotalPercent(s)` is horizon-capped now. See 8-BURN.
8b. **CLOSED in ticket 38** — `applyDamageModifiers`' self-target double-apply is fixed (deduped by id, like the heal side). It had made `core_overclock_daemon` 1.44× and `thermal_overload` 1.5625× against yourself only.
8b2. **THE EVAL NOW PENALISES YOUR OWN DEATHS (ticket 38) — do not re-derive this.** `evaluateState` had `+ (oppDead * 50)` with no counterpart, so **a mutual kill evaluated as a WIN**: a dead unit scores 0, the concave HP curve makes a nearly-dead one nearly worthless anyway, and trading ~63 points for `oppScore + 50` is always positive. hel_v2's mutual kills ran at 61% before the fix and 35% after. The symmetric `- (myDead * 50)` is deliberately conservative — **the truer term is that losing your LAST unit in 1v1 is a loss, not −50**, and that one WILL move every matchup, so it needs its own ticket and a full re-baseline.
8c. **`multiplier: 0` used to be silently dropped** — `HookFactory` guarded with `if (multiplier)` and 0 is falsy. Fixed to `!== undefined` in ticket 36. If you write a data hook that zeroes something, that guard is why it works.
8c2. **`zod` STRIPS UNKNOWN KEYS — a hook-action field added to `HookTypes.ts` but not to `HookSchema.ts` does not exist at runtime.** `firmwareRegistry`/`daemonHooks` parse `hooks.json` through the schema, and a dropped field fails *silently*: ticket 36's `escalatePerPlay` produced three sim runs whose numbers were **byte-identical to the flat rates**, which is the only reason it was caught. If a new data field looks like a no-op, log it inside `HookFactory` before you tune anything.
8c3. **An escalating cost is ORDERED, and the AI games it.** hel_v2's toll rises with cards played this turn, so the search simply front-loads the expensive card and pays the base rate for it. Escalation taxes the tail, never the opener — if the thing you want to stop is an alpha strike, escalation is the wrong shape.
8c4. **The 3e budget band assumes you spent a whole turn's Energy.** `soul_tithe` scores 10.4/10.5 and that is correct for anyone paying 3 Energy. hel_v2 pays 12 HP and casts three more cards. Any future OS that removes the Energy constraint needs its payoff cards hand-priced, not curve-priced.
8c5. **`percentMaxHP` now floors the PRODUCT, not the percentage.** Every registry `scaling` had been an integer, so it never mattered; the first fractional factor put 22.5 HP of damage into an entity.
8d. **Cost hooks are NOT run by `getEffectiveCardCost`.** The reducer applies `onCostCalculated` separately in `handlePlayProgram`, so anything that prices a card independently must run it too. `TacticalAI` and `CardHand.tsx` both did not, and both were fixed in ticket 36 — the AI skipped a 3e card it could afford, and the UI rendered it greyed out as unplayable.
6. **Sköll is CLOSED (ticket 31).** The cause was the deck's cost curve against a 2-Energy economy, not the stat line and not the daemon: three 2e cards in a 9-card deck makes "0e + one 2e" the whole turn, every turn, locking out all five 1e cards. Fixed by cutting `overdrive`, re-costing `brute_force` 2e → 1e (25 power, +8 with Strength), and softening the stat line to 70/95/55. **0.690 → 0.640, dead 50.9% → 32.3%.** Two useful negatives: a SINGLE 2e swap never clears the band (best is 43.0%), and enlarging the deck moves §2.3 a lot but dead cards barely — the metric counts instances that reached a hand, so a bigger deck just means more instances seen.
7. **STILL OPEN — TREACHERY_KERNEL over-feeds.** Peak Strength on skoll_v1 measures **13.7 stacks in 3.4-turn games** (16.5 at 4.1) against a 12.5-stack damage cap and an 8-stack cap on `CORE_OVERCLOCK`'s scaler. The ramp is pinned by turn 2 and the rest is discarded — which is why dropping that multiplier 1.2 → 1.10 moved the mirror by 0.02 turns. "Gets scarier the more it is hurt" never plays as a *curve*. Nothing in the card layer can fix that; it is a firmware-generosity question and it is the natural next sköll ticket.
8. **`brute_force`'s OS-guaranteed-conditional redline is closed** by the re-cost. It took the 0.7 uncertainty discount while the OS made Strength near-certain — priced as certain it was 72 power against a 65 cap; at 25+8 it is 33 against 30. **The general problem remains:** powerscale is per-card static analysis with no deck or OS context and will never flag this class. Check it by hand whenever a conditional's trigger is something the firmware supplies.
9. **Henry's law for per-stack scaling attacks (ticket 32):** they should **underperform early and overperform late** — that is the shape, not a bug. **Never cap pre-emptively.** `STRENGTH_STACKS`' cap was added AFTER measurement showed `momentum_crash` at 29.3 dmg/play; `DAZED_STACKS` shipped uncapped and needed no cap (`slander` measured 13.7 stacks at cast for 16.8 dmg/play — on rate). Corollary: **a scaling card's static score is a FLOOR, not a price** — powerscale prices at `ASSUMED_STATUS_COUNT = 3` while the deck built around the card sees 13.7. Always hand-price against the deck's realistic count and say so in the ticket.
10. **powerscale scores daemons now (ticket 32).** They carry empty `actions`, so all nine used to score 0.00 and the "Daemon Premium ×1.5" multiplied nothing. Hook `do` actions are scored once × `EXPECTED_DAEMON_PROCS = 4`; `GENERATE_CARD` is priced as the generated card's own score, recursively, with a `seen` guard. Also a floor — `echo_chamber_v2` scores 4.90 but runs at ~2× that in the deck built for it. **Hooks with no `do` array still score 0 and that is correct** (`core_overclock_daemon` is a multiplier, `einherjar_standard` a passive — the model declines to invent a number). **One new redline: `fertile_ground_daemon` 7.60 vs a 6.5 band** — follow-up ticket, not re-tuned.
11. **Check the prompt's baseline before trusting it.** Ticket 32's prompt said ratatoskr failed three of five bands; at HEAD it failed none, because ticket 30 had given `pollen_cloud` (in his placeholder list) real damage. It also sized `slander` against a `crippling_vine` that ticket 30 had reverted 8 Dazed → 2. Re-baseline first; a prompt written against an older commit ages fast right now.
12. **`scorch` was collateral damage from the Burn overflow fix** — a 2e card applying 4 Burn, whose 4th stack now overflows for nothing. skoll_v2's whole Burn plan delivers only 4.9 HP/game residual. Re-cost it.
13. **CORRECTION to ticket 29's "model blind spot" list.** `scry`, `keen_edge`, `soothe`, `spiked_carapace`, `equilibrium`, `acid_splash`, `curse_mark` are NOT blind spots — DRAW (15 power/card), ENERGY (20/point), HEAL, BarkShield and Poison are all priced, and all seven return an empty `manualReview`. They are ordinary rework candidates. The genuinely unpriced set is three cards using `MANUAL_REVIEW_TYPES`: `scavenge_data` (SEARCH), `reprogram` (PLAY_LAST_CARD), `purify` (CLEANSE) — they score 0.00 and flag themselves.
14. **CLOSED in ticket 47** — negative `stacks` now gets its own sign flip, the same shape as ticket 43’s `consume` flip. `soothe` −0.80 → **+0.80**. Any future "shed N stacks" card prices correctly; before this every one of them scored negative. See 8-REMOVAL for the cap that came with it.
15. **The status-percentage question is CLOSED — do not reopen it.** 2%/stack and the 25% cap stay. Raising to 4.5% was derived, built, measured and reverted: it shrinks the cap from 12.5 stacks to 5.6, and it silently multiplies **13 OS/daemon hooks by 2.25x** with none of it visible to powerscale, because firmware is not card data and is never audited. Status cards that cannot fill a budget on stacks spend the rest on a SECOND EFFECT — riders are cheap: ATTACK 10 power = 1.0 score, HEAL 10 = 0.75, DRAW 15/card, ENERGY 20/point, BarkShield 4/%maxHP, Stunned 55.
16. **`Side` scope on a debuff is dangerous.** The AI's targeting bucket sends `Side`/`All` cards to BOTH parties, so a side-scoped debuff can be aimed at your own Mingming. A 0e side-wide Daze draft of `disorienting_gust` cost sleipnir 0.310 → 0.260 before it was re-cut single-target.
17. `corrosive_leak` +1.8 budget redline, open since ticket 20.
18. **Deferred by decision:** type-matrix tuning + AI-determinism (blowouts are legacy deck quality, not the matrix); valkyrie v1 team measurement (needs ticket 05 team scenarios); fafnir/gullinbursti split (settles in the Earth pass).

- **Ticket 53 (Light) LANDED 2026-08-12 — 32/32 decks live.** Three things in it need Henry and nothing downstream should assume they are settled:
  1. **REBIRTH_CYCLE_OS was capped at once per turn, which is beyond the ticket's authorised knobs.** As specified ("every reshuffle") it killed the control on turn 1 with six procs, because valkyrie_v2's 8-card deck reshuffles on nearly every draw and `glimmer` is a 0-cost draw. The guard is a design decision awaiting review, not a knob.
  2. **§2.3 is out of band on both Light species with both knob budgets spent** — valkyrie 0.170 (v2 is the roster's strongest deck at 95.0% field, next is 85.0%), audhumbla 0.000 (v1 100/100; both knobs were a measured no-op, the same structural-not-numeric pattern ticket 52 hit on gullinbursti).
  3. **NOURISH_ROUTINE's dial is priced in the wrong unit.** It converts a percentage of heal POWER, but the engine converts power to HP at `maxHp × power / 400` — so `sacred_spring`'s 90 power is 22 HP, and 30% of that is 6 damage for 2 Energy. Raising the percentage cannot fix it. **This is the single most useful open finding in the ticket.**
8-FLOOR-REGEN. **TICKET 49's TABLES ARE REGENERATED at `1:e2f392b8` (2026-08-12) — §2b, §2c, §3b.** The floor list is down from eight decks to seven and its composition changed completely. **Read §2c first: it is the FIELD round robin the ticket asked for and never had.** The two tables disagree, hard: **jormungandr_v2 clears the floor at 0.04 and is DEAD LAST in the roster against the field at 9.0%** — ticket 51 fixed its control number and fixed nothing about the deck. **`kraken_v2` (0.53) is the entry to take first**, because it is the only one that moved by zero games across every ticket since the original reading — no instrument artifact in it at all. And **the floor itself moved**: the slot-1 aggregate is 0.114 against the 0.237 ticket 45 calibrated the control to, so "loses to the control" does not mean what it meant.
8-DECKREPORT. **THE FIRST FULL 32-DECK REPORT EXISTS** — `npm run balance:deck --iterations 80`, at `1:e2f392b8`. Three classes of finding nothing else reports: **(a)** it names the card inside a dead-card ratio — hræsvelgr_v2's 48.4% is `tailwind` 79% dead, `cinder_gust` 58%, `zephyr_strike` 56%; **(b)** 21 `POWER_DIVERGENCE` rows where measured value is far off the static score, worst being `hexbloom` (prices 6.3, measures 63.0 in huldra_v1) and `wither_feast` (prices **−1.8**, measures 12.8 in nidhoggr_v1); **(c)** **3 FTKs each for hel_v1 and hel_v2 over 480 games** where the balance suite's 100-game sample reports 0 — HANDOFF's "~1.6%" line now measures ~0.6%.
- **A `COUNTER` action in `hooks.json` with no `"target"` field is silently dropped.** `HookFactory.executeActions` early-`continue`s any non-LOG action whose target resolves to null, and a bare `COUNTER` resolves to null. Every pre-53 hook happens to carry `"target": "SELF"`, so nothing had hit it. Same failure mode as 8c2 (zod stripping), different layer — **check the counter actually moves before concluding a guard does not work.**
- **`runBatch.test.ts`'s stalemate fixture has run out of species.** Every mirror on the roster now decides (ticket 48 retired `draugr`, ticket 53 retired `audhumbla`, the last one). It is now synthetic by construction — `ymir`'s ~14-turn mirror truncated at a cap of 6 — rather than a species that stalls forever. Do not re-point it at a species.

0-COMPENSATION-FIXED-POINT. **When you change what a card scales off and "compensate" the power, the compensation is a FIXED POINT, not a ratio.** `getBestAction` pushes candidate plays through the reducer and prices a card as it will actually resolve, so raising the payoff makes the AI *re-sequence* to feed it. Ticket 71 measured `ink_stream`'s triggered-draw mean rise from 0.92 to 1.33 purely because its power went 12 -> 48, and the naive ratio-derived power over-delivered by **77%**. Sweep the power and read the delivered damage back; never multiply by a pre-change ratio and ship it.
0-COUNTERFACTUAL-SAMPLE. **A statistic measured on the OLD behaviour does not describe the NEW behaviour, even when it is measured on the exact quantity you are about to switch to.** Ticket 71's pre-fix census said 41.5% of `ink_stream` casts had zero triggered draws, and that was used to flag a severe variance change. Post-fix it is **12.9%** — the pre-fix number counted triggered draws on casts the AI chose for entirely different reasons. Flag the risk, but do not report the counterfactual number as the outcome.
0-DECK-NOT-CARD. **A shared card cannot be priced right for two decks that earn different amounts of its resource, and the mismatch reads as a card problem when it is a deck problem.** Ticket 71's `ink_stream` at one power gave `jormungandr_v1` 17.1 damage a cast and `kraken_v1` 8.1, because `kraken_v1`'s advertised "draw engine (4 draw cards)" is two 1-draws and two draws **conditional on Dazed** — 0.92 triggered draws a cast against jormungandr's 1.75. The bug had been paying her for the draw-phase refill and hiding it. **The fix was one 0-cost card (`water_slap` -> `undertow`), and it moved her from beating control 41% to 96%.** Before tuning a card's number, measure whether each carrier deck can actually feed it.
0-TOTAL-NOT-PER-CAST. **Per-cast and total delivered damage are different gates and they disagree.** Cast counts move when a card's shape changes (the AI plays a payoff card less often when it has no fuel). Ticket 71: preserving per-cast damage wanted power 24, preserving total wanted 33. **Total is the balance-relevant one** — it is what moves a win rate. State which one you held.

0-BAND-CENSUS. **THE 10-90% STANDARD FAILS ON 46.5% OF THE ROSTER (ticket 69, 480 cells, 28,800 battles, at `198ac2c`).** 110 cells above 90%, 113 below 10%, **52 at an absolute 100% and 56 at an absolute 0%, and not one deck of 32 is clean.** The distribution is **bimodal** - 231 cells in the two extreme deciles, 19 in the 50-60% one - so the roster is rock-paper-scissors with a thin middle, and **every gate we have is an aggregate that cannot see this shape.** Data: `docs/balance/matchup_band_census.json`; instrument `scratch/bandcensus.ts`.
0-TYPE-SWING-61. **Elemental advantage is worth 61.5 points of win rate** (within-deck, 24 decks with all three buckets: ADV 80.9% / NEU 46.2% / DIS 19.3%). That is the measured size of a x1.5 multiplier with no resistance, and it is the largest single lever in the game. **But 57.4% of band violations are NEUTRAL** - a perfect elemental fix leaves 26.7% of the roster out of band. Deck power spread is the bigger half. Do not treat the elemental shape change as the fix.
0-FTK-BLIND-SPOT. **The FTK gate has been reading 0 because the balance suite does not run the matchups where FTKs happen.** Ticket 69's full-field census found **43 FTKs in 14 cells**, with `jormungandr` on one side of 13 of them; `skoll_v1` vs `jormungandr` first-turn-kills in 1 game in 6, both directions. **"FTK 0" from `npm run balance` is a statement about 67 matchups, not about the game.**
0-AGGREGATE-BLIND. **A deck can be perfectly balanced on every aggregate and blow out every individual matchup.** `gullinbursti_v1`: 54.0% mean field win rate, **11 of 15 matchups out of band.** Field win rate, control floor and mirror all pass it. Only a per-cell census sees it.

0-STAT-LANE. **Stats move a matchup's LEVEL, not its VARIANCE (ticket 70, 10 arms x 1,800 battles, two seed bases).** On kraken: **+24 HP bought 6.5 points of field, removed none of kraken_v2's seven zero-win matchups, and moved net/turn by 0.04** - a bigger frame loses the same fights more slowly. **DEF bought the most field per point and made the band WORSE** (def111: best net/turn at -0.10, best dead-card ratio, and the only arm to raise v1's violation count, to 13) - defence lets her survive fights she was already winning. **ATTACK was the only lane that killed outright losses**: zero-win matchups 14 -> 6, v1's NEUTRAL zeros -> 0. Shipped **attack 80 -> 100**; 104/105 rejected because they convert LOW violations into HIGH ones at the same band count. When a deck's problem is 0%/100% cells, reach for a stat only to fix the level; the shape needs a design change.
0-KRAKEN-OUT-OF-BASEMENT. **Both kraken decks cleared the 0.60 control floor for the first time, and neither fix was a stat.** `kraken_v1` came out on ticket 71's `undertow` swap (control 59% -> 4% against her); `kraken_v2` on ticket 70's attack lane (control 46% -> 10%). Still open: `kraken_v2`'s field at 27.9%, below the 0.35 floor, and both decks' >90% type-advantaged blowouts, which every stat arm made worse.

0-SCALER-NEEDS-A-CEILING. **The per-event-count scalers (`CARDS_PLAYED`, `CARDS_DRAWN`, `CARDS_DRAWN_TRIGGERED`, `CARDS_DISCARDED`) were the ONLY scalers in the engine with no cap, and that is what made a first-turn kill reachable.** Ticket 73: all 43 FTKs were `jormungandr_v1` turn one - `undertow` x2 (0e draws) -> OUROBOROS_LOOP's +1 Energy -> `ink_stream` at **33 power x 3 triggered draws = 99 power from a 1-Energy card** against a 1e budget of 30. **The energy accounting was correct; the multiplier was the defect.** Shipped `DRAW_SCALING_CAP` 2 and `PLAY_COUNT_SCALING_CAP` 3 alongside `STRENGTH_STACK_CAP` 8 and `MISSING_HP_PCT_CAP` 50. **Any NEW scaler ships with a ceiling.**
0-MEAN-PRESERVING-TAIL. **A mean-preserving compensation moves the TAIL by far more than the mean, and the tail is where a first-turn kill lives.** Ticket 71 solved `ink_stream`'s power as a cast-weighted fixed point and flagged in its own §8 that one number cannot serve two decks of different draw density - then shipped. Reverting ONLY `ink_stream` to its pre-71 footing takes the full field from 43 FTKs to 0. **When a change raises a card's ceiling, measure the ceiling, not just the average.**
0-GATE-BLIND-SPOT-CLOSED. **`npm run balance` now scans all 480 cells** (`src/debug/balance/fieldCensusSuite.ts`, 2 shards, artifact `docs/balance/field_census.json`). FTK 0 is a hard assertion there. **Default is 10 iterations (~8 min) - a smoke alarm. `CENSUS_ITERATIONS=30 npm run balance` is the AUTHORITATIVE read (~28 min on two cores) and is what a ticket claiming FTK 0 must quote.** The band data is recorded but NOT asserted yet; Henry's neutral-absolutes gate is one line away when the queue lands.
0-ARM-HARNESS-LIES. **A sweep arm that derives one knob from another will silently ignore what you asked for.** Ticket 73's parser computed the play cap from the draw cap and overwrote the explicit value, so every arm labelled "play 5" ran at 3 and the 14-cell scan reported a clean zero. The FULL-FIELD scan is what caught it (3 FTKs). **Set each knob independently, and confirm a zero on the full field before believing it.**
0-RATATOSKR-V1-UNCAPPED. **`ratatoskr_v1` is `jormungandr_v1`'s pathology one energy tier slower** and ticket 73's cap exposed it: `seed_bomb_v2` x2 ("15 power per card played") behind four 0-cost cards plus `echo_chamber_v2`, which mints more 0-cost tokens. `os:ratatoskr` went **31% -> 0%** on the cap - that is where its power was. Never produced an FTK only because 15 power per card on a 2-cost body is not lethal on turn one. **Sequence its rebuild with the jormungandr_v1 cut - same disease.** The archetype web lists it Control/attrition; it measurably plays zoo/velocity.

0-NO-CAPS (Henry, 2026-08-17). **Caps on per-event-count scalers are REJECTED as a design shape.** *"I don't like caps, that makes playing smart feel bad and you'll end turn with energy. You should be rewarded for playing smart."* Ticket 73's `DRAW_SCALING_CAP`/`PLAY_COUNT_SCALING_CAP` are reverted. **Brake the ENGINE that feeds a scaler, never the scaler.**
0-SCALER-IS-SHARED. **A shared scaler cannot be tuned for one deck - every change lands hardest on the deck least able to use it.** `ink_stream` is carried by `jormungandr_v1` (3 triggered draws a turn) and `kraken_v1` (~1). Measured on the SAME first-turn-kill problem: ticket 73's cap cost **Kraken 3 points of field (42.0% -> 38.9%)** to take 13 off Jormungandr; cutting the rate to 12 power dropped Kraken's damage from the card to **306** and she stopped casting it. The OS fix cost Kraken **nothing** (44.9%). Before touching a shared card, list its carriers and their engine rates.
0-OUROBOROS-WAS-THE-FTK. **The first-turn kill was OUROBOROS_LOOP, not `ink_stream`** - it handed `jormungandr_v1` a free +1 Energy AND +1 draw on the 3rd Water card, on a turn where four of her nine cards cost nothing. **The DRAW was the lever and the Energy was not**: over the 14 census cells, removing the Energy alone took 43 kills -> 9; removing the draw alone took 43 -> 2. Now *"the 5th Water card you play draws 1 card."* Full field: **2 FTKs, both `skoll_v1` vs `jormungandr` (3.3%), accepted.**
0-ACCEPTED-FTK. **The field-census gate has a named allowance list (`ACCEPTED_FTK` in `fieldCensusSuite.ts`), not a loosened assertion.** One entry: `skoll_v1` vs `jormungandr`, up to 8% (measured 3.3%). Every other cell is a hard zero, and this cell getting worse still fails. **Adding an entry is a Henry decision and needs the rate, the type relationship, and what has to line up.**
0-JORM-V2-IS-NOW-THE-STRONGER. **`os:jormungandr` went 98% -> 34% on the OUROBOROS nerf: `jormungandr_v2` is now the stronger variant.** v1's FIELD is healthy at 50.9%, so this is a v1-vs-v2 relationship item, not a v1 problem. **This INVERTS the queued "jormungandr_v1 cut" entry - re-read it before actioning.**

0-WHEEL-DOES-NOT-TURN. **The archetype wheel is a LADDER, not a wheel (ticket 76, 960-cell deck grid, 57,600 battles).** Neutral cells, normalised against each deck's own average: **ZOO into RAMP -4.3 pts (INVERTED), RAMP into CONTROL +5.8 (holds), CONTROL into ZOO +26.1 but over only THREE cells.** Read by column: every role does better than usual into ZOO and worse than usual into RAMP. Role means **RAMP 58.5% > BURST 51.9% > ZOO 40.0% > CONTROL 35.0%.** **Root cause is role SIZE - ZOO 3 decks, RAMP 9, CONTROL 7, BURST 13.** Zoo x Control is 21 pairings and the type chart claims 18, so the closing leg cannot be measured. **The roles are real** (game length ZOO 4.8 turns, BURST 5.2, CONTROL 7.2, RAMP 7.3); only the counter-relationships are missing. **Reassign decks before tuning any of it.**
0-TYPE-IS-67-POINTS. **Deck-level, elemental advantage is worth 67 points** (ADV 83.7% / NEU 50.2% / DIS 16.7%) - up from the species census's 61.5, because a deck grid removes the smoothing. **Perfectly one-directional: no ADVANTAGED cell is ever 0% and no DISADVANTAGED cell is ever 100%**, and neutral sits dead centre. **The type chart is the healthiest system in the game and needs no work.**
0-DECK-GRID. **`docs/balance/deck_grid.json` is the deck-vs-deck artifact** (`scratch/deckgrid.ts`, 960 cells at 30x2). **Use it, not the species census, for anything role-related** - the species census puts the opponent on `availableOS[0]`, so half of every archetype was never on the board as an opponent and the web had literally never been measured. Field spread **24.4% to 81.4%**; **84 cells at absolute 0% and 83 at 100%, of which 38 and 38 are NEUTRAL** = 76 bucket-band gate failures.
0-QUEUE-NAMES-MISSING. **Three decks the queue does not have, from the deck grid:** **`fenrir_v1` is the worst deck in the game by cell count** (24.4% field, **14 zero cells**, 21/31 out of band) and is scheduled nowhere; **`ymir_v1`/`v2` hold 11 and 10 cells at 100%**, the biggest blowout dealers, never passed; **`audhumbla_v2` has 6 zeros AND 5 hundreds at a 44.0% mean** - the gullinbursti_v1 pathology exactly. Counterpoint worth keeping: **`hraesvelgr_v1` has ONE out-of-band cell in the entire grid** and is the model for what the standard looks like when it is met.
0-HEL-V1-STANCE-INVERTED. **TWILIGHT_CADENCE works and is pointed the wrong way round (ticket 75).** She deals **54.2%** of her damage from LIGHT stance (no offensive effect) and takes **66.3%** in DARK (no defensive effect). Stance sets at END of action, so a Dark attack leaves her in Dark for the opponent's swing and `pale_mercy` leaves her in Light for her own - **she cannot hold the right stance for both phases of a round, and tuning the +30/-30 cannot fix a phase error.** Two hypotheses DISPROVED: the stance does toggle (42.5% of actions), and **`eclipse`'s +30 lands on 82.9% of casts.** Her second problem is throughput - median ~13 damage/turn against `hel_v2`'s ~25 from the same body.
0-NET-PER-TURN-MISLEADS. **Net damage/turn is not comparable across decks with different clock speeds.** `hel_v2` is net-NEGATIVE against 9 of 15 opponents - including **-4.57 against a kraken she beats 92%** - and is the roster's joint-strongest deck at 81.4%. Ticket 67 used net/turn correctly on kraken because it compared her to a deck of similar pace. **Across paces, compare THROUGHPUT (damage/turn dealt), not the difference.**

0-HEL-V1-IS-THE-AI-NOT-THE-OS (ticket 77 - **RETRACTS ticket 75's headline**). Ticket 75 called TWILIGHT_CADENCE "structurally inverted" without separating "the OS is inverted" from "the AI never closes on Light" - the same measurement supports both. **Henry's read was right.** Playing the deck as designed (reserve the last Light card, close the turn on it) moves her **23.9% -> 29.2%** and takes damage absorbed in Light stance from **25.1% to 48.3%**. The OS is fine; setting stance on CAST would make the defensive half unreachable, because you are never attacked during your own cast.
0-AI-HAS-NO-END-OF-TURN-CONCEPT. **`getBestAction` never values the board state it hands the opponent, and that is a general defect, not a hel problem.** hel_v1 ends out of Light stance while HOLDING a castable Light card on only **5.5%** of turns - the other ~60% she has already SPENT her closer earlier in the turn. Reordering only the last play recovers a third of the value; reserving the closer recovers all of it (+5.3 points of field). **Any deck with an end-of-turn payoff is being underplayed the same way.** Next ticket.
0-STANCE-BONUS-KNOB. **`STANCE_BONUS` in `core/Hooks.ts`** - the Dark +30% / Light -30% were two literals inside `applyDamageModifiers` and are now a dial, shipped at 0.30/0.30. Measured on top of correct play: **Dark 30->50 = +10.5 points, Light 30->50 = +16.6, both = +27.2 (hel_v1 to 56.4%)**. **The DEFENSIVE half is worth more than the offensive one** once she actually holds Light through the opponent's turn.
0-PURIFY-IS-A-BLANK. **`purify` costs hel_v1 +7.2 points and its premise is disproved.** Swapped for a second `nights_bite`: 29.2% -> 36.4%. **She does BETTER against Poison/Burn decks WITHOUT it** (36.7% vs 30.8%), and ticket 75 measured it played essentially at random - 93 casts into DoT decks against 102 into decks with no DoT. An answer card that does not read the question is a blank.
0-ECLIPSE-IS-FINE-SHADOW-CLAW-ISNT. **`eclipse` is correctly priced and is hel_v1's BEST card** - score 6.10 against a 2e budget of 6.50, and its conditional **over**-delivers (the scorer assumes 70%, it lands **82.9%**). **9.4 damage per energy, 1.5x her next-best.** The correctly-priced 1e version measured WORSE (28.1% vs 29.2%). **The bad card is `shadow_claw`: 1,611 casts at 0.9 damage each**, her most-played card doing nothing. General lesson: a card that looks weak in absolute damage may be the best thing in its deck - always read damage/energy against its siblings before repricing.

0-AI-PRICED-STANCES-AT-ZERO (ticket 78 - the actual root cause behind ticket 77). **`TacticalAI.statusValue` had `default: return 0` swallowing the stances**, so ending a turn in LightStance scored identically to ending it in Dark. The AI was not sequencing badly - **it could not see the stance at all.** Fixed with two cases in the table that prices every other status: `LightStance` = ONE opponent turn of throughput x the reduction (one turn, not STATUS_HORIZON_TURNS - a stance lasts only until its holder casts the other element), `DarkStance` = that halved (Light pays on the opponent's certain next turn; Dark pays only on your own, only if you attack first, and same-turn Dark damage is ALREADY visible to the search, so full value double-counts). **The real fix beat ticket 77's hand-written reserve policy, 31.4% vs 29.2%** - it weighs the stance instead of blindly reserving. **The `default: return 0` branch still swallows StableOS, Awoken and every marker status; the same bug is one drafted card away.**
0-STACKED-FIXES-OVERSHOOT. **Knob values measured against a BROKEN baseline do not survive fixing the baseline.** Ticket 77 measured stance 0.50 on top of the broken AI with `purify` still in the deck; with those two fixed, 0.50 put hel_v1 at **74.0% field with EIGHT cells above 90%** - second-worst deck to fourth-best, trading low-end blowouts for high-end ones. Re-swept: **0.35 is the only value with no absolute in either direction** (59.8% field). **Re-sweep every knob after any other change lands.**
0-HEL-V1-FIXED. **hel_v1 24.7% -> 59.8% field, 5 zero cells -> 0, 8 band violations -> 4** (ticket 78: AI stance pricing + STANCE_BONUS 0.35 + `purify` -> a second `eclipse`). She beats the control deck 100% and her mirror is even. **The roster got healthier too**: absolute 0% cells 84 -> 80, 100% cells 83 -> 78, NEUTRAL absolutes 38/38 -> 34/33, band violations 420 -> 411. Other decks moved -1 to -2 points, which is just the arithmetic of one deck getting stronger. **Still open: `os:hel` is a 2.3 redline at 17%, and `hel_v2` at 81.4% with four 100% cells is untouched.**
0-ILLEGAL-ARM. **Check a candidate against the DECK RULEBOOK before quoting its number.** Ticket 77's headline `purify` fix (+7.2 points) swapped in a THIRD `nights_bite`; the rulebook caps copies at 2, so that arm was never shippable. The legal re-sweep picked a different card entirely (a second `eclipse`, 46.7%).

0-METHOD-DOC. **[METHOD.md](METHOD.md) is the process document; this file is the findings log.** Henry asked for a running lessons-learned so passes stay consistent - how to diagnose, which instrument answers which question, the failure modes we have actually hit, and the shipping discipline. **Read it before starting a balance pass.** When the two disagree, HANDOFF is newer.
0-TURN-THE-OS-OFF. **The single most useful diagnostic on a strong deck is deleting its OS and re-running the field** - it separates "strong deck" from "strong OS", and the answers differ wildly: `hel_v2` 81.4% -> **1.8%** (the OS IS her economy), `ymir_v2` 81.3% -> 60.1%, `nidhoggr_v2` 75.7% -> 66.9%, `nidhoggr_v1` 78.2% -> 70.9%. **Three of the four would still be top-eight with no OS at all**, so an OS nerf alone cannot fix them. **GOTCHA: an OS can live in TWO places** - `initFirmwareHooks` concatenates `hooks.json`'s `hooks` with `CustomFirmware[key]`. Clearing only the JSON side measured hel's healing bonus alone and measured NOTHING for ymir.
0-PAYOFF-NOT-SHARE (Henry, 2026-08-17). **Damage share does not diagnose anything** - decks are built around one payoff on purpose. Measure **how EASY the payoff is** (first-cast turn, casts a game) and **how HARD it hits** (damage as a share of the target's WHOLE health bar, as a distribution). `nidhoggr_v1`'s `wither_feast` is 80% of her damage and is HEALTHY on accessibility - 0.63 casts a game, first cast turn 4.4. `hel_v2`'s `soul_tithe` is 43% and is the problem - turn 1.8, twice a game, 32% of a health bar, **no gate at all**.
0-HEL-V2-HAS-NO-GATE. `soul_tithe` (3e, 90 power) costs 15% max HP against UNDERWORLD_GATEWAY's 20% turn budget, so it casts **turn one and every turn after** while her 2 Energy stays free for Light cards - and the OS's +50% healing refunds the blood. **The blood price is a loan she repays with her own cards.** The 20% turn cap is INERT: at 15% it still does not bind, because `soul_tithe` costs exactly 15%. **The only knob that works is the PRICE: 5% -> 6% per Energy = 80.7% -> 67.6%.**
0-YMIR-V2-DRAWBACK-INERT. **`maxCardsPerTurn: 2` has never done anything** - ticket 50 documented it in `CustomFirmware.ts` and it was never acted on. **Measured: she plays 1.06 cards a turn.** The Ice bonus has been walked 50% -> 35% -> 25% across three tickets without fixing her. **Make the drawback real (cap -> 1) = -15.4 points**, and it changes her actual play by a quarter of a card a turn. **Do not shave the bonus a fourth time.**
0-CONDITION-DIRECTION. **`minStacks` on a maintenance hook is BACKWARDS.** ROOT_CORRUPTION gated on the Poison pile being LARGE fires MORE in the late game, which is the opposite of "triggers less" - rootmin 2/3/4 moved `nidhoggr_v1` only 78.2% -> 75.3%. The shape needed is a **maximum** (stop maintaining a pile once it is big), and the hook schema has no `maxStacks`. **When adding a condition to make an OS trigger less, check which direction the game state moves over a match.**
0-POWERSCALE-CANNOT-SEE-FIRMWARE. **Every Ice card in `ymir_v2` is worth 25% more than its printed score and the card audit does not know**, because the bonus is firmware, not card data. True of any damage-multiplying OS. **A card-budget pass on a deck with a multiplier OS is measuring the wrong number.**

0-TOP-FOUR-NERFED (ticket 80). **`ymir_v2` 81.3 -> 69.0** (`maxCardsPerTurn` 2 -> 1), **`hel_v2` 81.4 -> 71.0** (blood 5% -> 6%, cap 20 -> 25), **`nidhoggr_v1` 78.2 -> 68.2** and **`nidhoggr_v2` 75.7 -> 69.1** (species defense 80 -> 68). No card touched. **Largest roster-health gain measured: absolute 0% cells 80 -> 71, 100% cells 78 -> 70, NEUTRAL absolutes 34/33 -> 26/26, band violations 411 -> 369.** 25 decks moved >=1 point and every one moved UP.
0-CAP-ON-A-REGENERATING-RESOURCE. **Removing `hel_v2`'s 20% blood cap while RAISING the price 5% -> 6% made her STRONGER, 81.4% -> 87.0%.** Uncapped she chains Dark casts and the OS's +50% healing refunds blood faster than the price takes it - **a cap on a resource you can regenerate is the only thing bounding the loop**, not an arbitrary restriction. Henry's fallback ladder was right: 25% -> 71.0%, 20% -> 70.0%. **The cap moves in ENERGY-POINT steps, not percent steps: at a 6% price, 18% and 20% are identical.**
0-SIMULTANEOUS-NERFS-UNDERSHOOT. **Single-deck arms over-state a nerf that ships with others.** Ticket 80's four knobs predicted 67.6/65.9/67.0/67.3 individually and landed **71.0/69.0/68.2/69.1** together - every one 2-3 points higher, because each nerf makes the other three easier for everyone else. **Rank with single-deck arms; measure the chosen set together before believing a number.**
0-YMIR-V1-IS-NOW-TOP. **`ymir_v1` is the strongest deck in the game at 77.1% with 10 cells at an absolute 100%**, promoted by ticket 80 nerfing her sibling (`os:ymir` 62% -> 98%, the one added redline). Her mechanism is known: **`avalanche`, "9 power per Bark Shield stack", explicitly UNCAPPED, behind two 0-cost `frost_ward`s and two `rimeguard`s** - the THIRD instance of the 0-cost-engine-into-unbounded-multiplier shape after `jormungandr_v1` and `ratatoskr_v1`. Caps are rejected as a shape, so the lever is the rate or the shield the engine makes. **`hraesvelgr_v2` at 75.2% is second and has never had a pass.**

0-ROUND-TWO-NERFED (ticket 81). **`hel_v2` 71.0 -> 55.9** (healing bonus REMOVED), **`hraesvelgr_v2` 75.2 -> 64.7** (UPDRAFT waits for the 2nd cycle), **`ymir_v1` 77.1 -> 69.6** (GLACIER_HEART 5 -> 4 shield), **`valkyrie_v2` 70.3 -> 63.8** (REBIRTH 15 -> 12). **All four are OS changes; no card, deck list or stat touched.** Roster: absolute 0% 71 -> 64, 100% 70 -> 63, NEUTRAL absolutes 26/26 -> 21/20, violations 369 -> 357, redlines 55 -> 53 (`os:hel` and `os:hraesvelgr` cleared). **The top of the ladder is now FLAT - six decks between 64.7% and 70.3%.**
0-HEAL-BONUS-BREAKS-HP-COST (Henry, 2026-08-18). **An OS that charges HP and boosts healing is charging nothing** - the cost becomes a loan, which is why removing `hel_v2`'s turn cap made her STRONGER. Bonus removed; `dawns_respite` now costs 12 HP and heals 12, net zero. **Design note for the next pass: her heals should all be RIDERS on attacks, never standalone cards, so a heal is never a blank when incoming damage exceeds it - and that change "might allow us to lift the cap".** `pale_mercy` is the one pure heal left; `dawnstrike` and `leech_strike` are already riders. The cap machinery is kept in `CustomFirmware.ts` for that experiment.
0-OS-CONTRIBUTION-LADDER. **Measured OS worth, seven decks: `hel_v2` +79.6, `hraesvelgr_v2` +64.4, `ymir_v1` +55.7, `valkyrie_v2` +34.5, `ymir_v2` +21.2, `nidhoggr_v2` +8.8, `nidhoggr_v1` +7.3.** A deck whose OS is worth +7 cannot be fixed by an OS knob; one worth +64 cannot be fixed by anything else. **Sub-signal: if the payoff card's damage barely moves with the OS off (`valkyrie_v2` 14% -> 13%), the OS is not ENABLING the payoff, it is adding value directly** - so the knob is the OS's own numbers, not the card.
0-YMIR-V1-OS-FEEDS-THE-SCALER. **She holds a mean 9.9 Bark Shield when casting `avalanche` and only 5.1 with GLACIER_HEART off** - the free 5-a-turn roughly doubles the pile, which doubles the payoff (median 30% of a health bar and **max 81%**, against 13%/56% without). Cut to 4 a turn. **`avalanche`'s uncapped 9-power-per-stack rate SURVIVES this pass and is still the largest single hit measured anywhere.** She also still carries 5 cells at 100% and 15 out of band, the most of any top-six deck - her field number is fine, her SPREAD is not.
0-PROBLEMS-ARE-NOW-AT-THE-BOTTOM. After tickets 80-81 the roster's remaining blowouts are concentrated in the weak decks, not the strong ones. **`fenrir_v1` at 24.9% with 12 zero cells and 22 of 31 out of band is by far the worst deck and the single biggest source of blowout cells** - never had a pass, and the obvious next job. **`audhumbla_v1` has risen to 70.1% untouched**, purely from everything around it being cut; worth a look before it becomes the next offender.

0-BOTTOM-FOUR-BUFFED (ticket 82). **`fenrir_v1` 24.9 -> 36.0** (UNBOUND_KERNEL recoil REMOVED, Strengthened 1 -> 3), **`kraken_v2` 25.8 -> 36.6** (TIDAL_CRUSH gate 3e -> 2e and bonus 15% -> 30%), **`fafnir_v1` 30.5 -> 40.7** (species attack 62 -> 68 + `slag_shed` -> a 2nd `motherlode`), **`fafnir_v2` 33.7 -> 39.2** (the shared attack stat). Roster: absolute 0% cells **64 -> 51**, 100% **63 -> 54**, NEUTRAL absolutes **21/20 -> 14/12**, out-of-band cells **357 -> 317**, decks inside the 35-80 band **28/32 -> 31/32**, FTK unchanged at 2. **Biggest single-ticket roster-health gain so far**; `fenrir_v1` alone went 12 zero cells -> 4. Everything untouched drifted down 0.3-3.3 points.
0-THE-BOTTOM-HAS-NO-OS-LEVER. **OS-off readings for the bottom four: `fenrir_v1` +0.9, `kraken_v2` +6.1, `fafnir_v1` +8.7, `fafnir_v2` +12.1** - against +34 to +64 for the nerf group. **The top of the roster is decided by firmware and the bottom by frames and cards.** On a weak deck the OS-off number answers "is there an OS lever here at all", and usually there is not.
0-TWO-WAYS-A-WEAK-OS-FAILS. **(a) A price with no product** - `fenrir_v1` paid 2% maxHP per attack for 1 Strengthened and cast her payoff MORE often with the OS OFF (1.89 vs 1.64). **On a 66 HP frame 1% and 2% round to the same damage, so the price had no intermediate setting** (`recoil=1` measured identical to baseline) and cost-gating it to 2e+ attacks bought nothing (25.3/26.5). **(b) A product with no price the frame can pay** - TIDAL_CRUSH paid only on 3e+ Water cards on a **2-Energy** frame, so `maelstrom`/`hydro_blast` cast **0.47/0.58 times a game**. Gate alone +2.1, bonus alone +5.5, **together +11.9** - a gate the deck cannot clear and a bonus too small to notice are ONE problem.
0-THE-STAT-BLOCK-IS-SOMETIMES-HONEST. **Both fafnir OSes work (+8.7/+12.1) and no OS knob moved anything (+-1).** His attack was **62, the lowest of the 32 decks**, on the element with the largest card pool - the ticket-52 registry comment had said so all along. 62 -> 68, not 72, because **a shared stat block must be priced for the STRONGER of the two decks** (72 put `fafnir_v2` at 45.3%). **`slag_shed` was 72% dead, 0 damage, measured 0.0 power - the deadest card found in any audit**; replaced with a 2nd `motherlode` (what HOARD_PROTOCOL banks Energy to cash into) for +4.0 alone. **But check a swap does not slow the deck**: fenrir's `ember_mend` swap was worth +0.9 and cost 0.2 cards/turn plus half a turn of payoff delay - a near-blank card wants a RIDER rewrite, not a swap.
0-NEW-FLOOR-IS-SLEIPNIR-V2. **`sleipnir_v2` at 33.7% is the only deck left under 35**, and it got there by standing still while the four below it were buffed. **`kraken_v1` (41.3%) is now the weaker kraken** and `os:kraken` flipped 72% -> 39%. Still near-blank and unaddressed: `hoardbreaker` (0.14 casts/game on a 5.6-turn deck) and `ember_mend` (played in 97% of games, heals ~1.6 HP).

0-SLEIPNIR-V2-BUFFED (ticket 83). **`hoof_strike` 12 -> 15 power** - the 0-cost token WAR_STEED_OS generates, which is in no deck list. **33.7 -> 42.4**, her zero cells 1 -> 0 and out-of-band 10 -> 5. Roster: absolute 0% **51 -> 50**, NEUTRAL 0% **14 -> 13**, out-of-band **317 -> 306**, FTK 2. **ALL 32 DECKS ARE NOW INSIDE THE 35-80 FIELD BAND for the first time.** 8-DIFF one row of 67 (`os:sleipnir`), redlines unchanged at 52.
0-A-DISCARD-DECK-WITH-NO-DISCARD-PAYOFFS. **`sleipnir_v2`'s `lance` and `cavalry_charge` discard at random and not one card in her list rewards being discarded** - while the Air pool holds four that do (`war_molt` +2 Strengthened, `feather_cache` +1 Energy, `sky_burial` rises 15 -> 30 -> 45, `carrion_swoop` 11 power per discard). Wiring in ONE is worth **+13 to +38 field points**; `sky_burial` alone took her to 70.0%. **`tailwind` is the card that does not fit - 47% dead, 34% played, zero damage, a draw card on a deck whose OS already fills the hand** - and `tailwind` -> `war_molt` measured 45.9%. HELD for Henry: deck lists are his review, and war_molt borrows sleipnir_v1's Strengthened package.
0-FENRIR-IS-A-BERSERKER-NOBODY-PAID (ticket 83, reported not shipped - [research/fenrir-berserker.md](research/fenrir-berserker.md)). Three compounding causes, not one. **(a) Half the deck fights the plan** - four of nine cards pay for being hurt, four pay for being healthy, and `blood_rite` x2 *loses half its damage below 50% HP and heals you back over the threshold*. **(b) The recoil is ONE HP and always was** (`max(1, floor(66*0.02))`, and 1% floors to the same 1): across a game it supplies ~8% missing HP, worth +5.6 power on `ragnarok_edge` - **the OS sold ~5.5 HP of her health for ~1.9 HP of damage**, and `berserk_rush` needs 50% missing so the OS never switched it on. **(c) The AI is built to leave the state she wants** - `getEntityScore` prices HP concave (85% sqrt) and ticket 27's comment names THIS deck as the reason.
0-SCALED-BEATS-FLAT. **Same +20% ceiling on Fire attacks: FLAT measured 34.8% (below the shipped build), SCALED BY MISSING HP measured 40.1% with the original recoil and Strengthened restored, and halved her zero cells 11 -> 6.** A flat bonus is a power increase that prices in against everyone equally; a bonus that pays when you are hurt turns hopeless cells into contests. **The recoil was never the problem - it was unpaid.** Ladder for Henry's pick: scaled 50 = 40.1, 60 = 44.7, 50 with 2 Strengthened = 46.9, 75 = 52.4, on ticket 82's OS = 56.0.
0-THE-PRICE-CANNOT-GROW-ON-A-SMALL-FRAME. **Raising fenrir's recoil to 8% (5 HP an attack - enough to actually REACH the berserk threshold) collapsed her to 20.2% with 19 zero cells**, games a turn and a half shorter. On the roster's smallest frame the self-damage has to stay near zero and the missing HP has to come from the enemy. **That is why the berserker never worked: the only version of the price that reaches the payoff also kills her.**
0-A-VOLUNTARY-HP-COST-NEEDS-AN-AI-CHANGE. Nothing in the card pool lets a Mingming spend HP on purpose (hel does it through her OS). A new 0e `feral_bite` (lose 8% max HP, gain 2 Strengthened) measured **+0.3** - the concave HP scorer will not pay for it. **Card-level berserk fixes all measured flat**: `blood_rite` flipped 35.2 (it behaves exactly as designed - more payoff casts, longer games - and still moves nothing), `bloodlust` swap 35.9, `feral_bite` 36.5.

0-BERSERK-CLAUSE-SHIPPED (ticket 84, Henry's pick). **`fenrir_v1` 35.5 -> 38.9**: UNBOUND_KERNEL's ORIGINAL text restored (1 Strengthened + 2% recoil) plus *"Fire attacks deal up to 50% more damage, scaled by how much of your max HP is missing"*, as firmware (`OS_KNOBS.fenrir.berserkPct`) because `MISSING_HP` scaling reads the TARGET's missing HP, not the owner's. Her zero cells 4 -> 3 AND her 100% cells 4 -> 2 - winning more and blowing out less. **`sleipnir_v2` 42.4 -> 57.0**: `tailwind` -> `war_molt`. Roster: 100% cells **54 -> 48**, out-of-band **306 -> 297**, 0% cells 50, FTK 2, redlines 52 -> 51, all 32 decks in band, spread now **36.1 to 68.7 - the tightest measured**. 844/844 tests.
0-A-SUBSTITUTE-FIX-STACKS-WITH-THE-REAL-ONE. **`war_molt` measured 45.9% alone against the pre-83 build and landed 57.0% once ticket 83's `hoof_strike` 12 -> 15 was live** - the token buff had been chosen only because deck lists are Henry's review, so the two are the same repair applied twice. **When an interim fix ships and the real fix is approved later, re-measure the pair before shipping - the arm was taken against a build that no longer exists** (3.4 / 4.3, and the buff-side twin of 0-SIMULTANEOUS-NERFS-UNDERSHOOT). Henry's call, asked mid-ticket: keep both.
0-SLEIPNIR-V1-IS-NOW-THE-GAP. `sleipnir_v1` 36.8 against `sleipnir_v2` 57.0 and `os:sleipnir` at 15% - the widest OS-variance gap on the roster. Diagnostic-only per the deep-phase policy. **Three Air discard payoffs are still unused in the pool** (`feather_cache`, `sky_burial`, `carrion_swoop`) and `sky_burial` alone measured 70.0% on v2, so the lever is there when she needs one.

0-STATE-OF-THE-ROSTER (ticket 85, full re-measure at `0ccc271` - [research/state-of-the-roster.md](research/state-of-the-roster.md), dashboard `docs/balance/roster_dashboard.html`). **32/32 decks inside 35-80, spread 36.1 - 68.7.** Against pre-79: 0% cells 80 -> 50, 100% cells 78 -> 48, NEUTRAL absolutes 34/33 -> **13/12**, out-of-band 411 -> 297, FTK 43 -> 2 (both accepted), spread 56.7 -> 32.6 points. **Field rate is no longer the interesting number - SPREAD is.** The decks that need work are picked out by out-of-band counts, not means: `audhumbla_v2` 16 (all neutral), `ymir_v1` 16, `kraken_v1` 16, `draugr_v2` 16, `skoll_v1` 15, `fafnir_v2` 15.
0-AUDHUMBLA-V2-IS-HALF-THE-BUG-LIST. **She is one side of 18 of the 25 remaining NEUTRAL absolutes.** 43.0% field but 16 out-of-band cells and every one of them neutral, on **11.2-turn games against a 5.2 median** - six decks never beat her, she never beats seven others. `audhumbla_v1` is simultaneously the strongest deck at 68.7% and `os:audhumbla` is a **100.0%** wipe, the largest OS gap on the roster. **She is the whole of the next pass.**
0-THE-WHEEL-IS-A-LADDER-AND-THE-ROLES-ARE-UNEVEN. Neutral-cell role matrix: RAMP 55.5 > BURST 50.3 > CONTROL 45.7 > ZOO 43.3, and **not one leg of ZOO > RAMP > CONTROL > ZOO holds** (ZOO loses to RAMP 34.3; CONTROL loses to BURST 33.8). Role sizes are **BURST 13, RAMP 9, CONTROL 7, ZOO 3** - a three-deck role cannot express a counter against a thirteen-deck one. **DESIGN CALL FOR HENRY: rebalance the role counts, or retire the wheel and let the type chart carry the strategy layer alone.** The type chart itself needs nothing: 84.1 / 50.1 / 16.0, one-directional, zero 0% cells in any advantaged matchup.
0-LONG-GAMES-ARE-WHERE-THE-ABSOLUTES-LIVE. `audhumbla_v2` 11.2, `valkyrie_v1` 8.0, `draugr_v2` 7.8, `huldra_v1` 7.7 turns against a 5.2 median - and all four carry double-digit out-of-band counts. **Past ~8 turns the better engine converts every time, which is what turns a 70/30 into a 100/0.** Pace is a candidate shared lever for the whole cluster.
0-DEAD-CARD-METRIC-HAS-TWO-BLIND-SPOTS. It counts a card sitting unplayed, so (a) it **cannot see discard value** - `war_molt` reads 62% dead on `sleipnir_v2` and is doing exactly its job - and (b) it **punishes a deck that cannot play its hand**: `ymir_v2`'s 0.605 ratio is mostly `maxCardsPerTurn: 1`. 32 of 211 rows are >= 35% dead; the honest worst are `hoardbreaker` 89%, `barrow_king` 86%, `ash_communion` 83%, `all_in` 82%, `tailwind` (hraesvelgr_v2) 78%. **And 28 of the 42 card-budget redlines are 0.1-0.4 over - noise.** The real ones are scorer blind spots: `hexbloom` static 16.5 vs measured **63.0**, `wither_feast` -10.8 vs 12.8.

0-ZOO-PREMISE-IS-FALSE (ticket 86 - [research/zoo-deep-dive.md](research/zoo-deep-dive.md), branch `archetype-web`). **Playing more cards does not deal more damage.** `calculateDamage` divides by the global pace constant 45 AFTER scaling by power, so cheap cards deal proportionally low damage and there is no volume bonus anywhere in the formula. Measured cards/turn x dmg/card: `sleipnir_v2` 2.31x6.5=15.1, `fenrir_v1` 1.51x8.7=13.1, `jormungandr_v1` 2.81x4.6=13.1, `sleipnir_v1` 2.93x4.4=12.9, `hraesvelgr_v1` 2.92x3.9=11.4. **Everyone deals 11-15 damage a turn.** ZOO pays for its volume in draw dependency, hand size, deck cycling and NO SPIKE (payoffs land for a median 7-11% of a health bar against burst's 25-41%) and the damage formula gives it nothing back.
0-NO-PREMIUM-FOR-THE-SETUP. `serpents_coil` 10 power per card played, `stampede` 11, `carrion_swoop` 11 - **about 30 power at three cards, which is exactly a 1-Energy card's budget.** A card that needs the whole turn assembled first pays what an unconditional card pays. Raising the rate moves all three decks: `stampede` 11->16 took `sleipnir_v1` **34.5 -> 68.8**, `serpents_coil` 10->15 took `jormungandr_v1` **49.6 -> 64.5**, `carrion_swoop` 11->16 took `hraesvelgr_v1` **41.6 -> 50.7**. Diagnostic dials, NOT ship values (+5/card is far too much for sleipnir; a tuning pass would land +2 to +3). No new mechanic, no cap, no deck change, no pace change - the machinery is right and already uncapped (ticket 74); it is priced as if the volume were free.
0-TWO-ZOO-HYPOTHESES-DISPROVED. **(a) "chip damage cannot break regenerating defences" is WRONG** - correlation between damage-per-card and win rate vs the six sustain decks is NEGATIVE (r = -0.44, n=26), and `huldra_v1` deals **1.1 damage per card** and beats the sustain cluster **76.1%**. **(b) "`sleipnir_v1` overfeeds the Strengthened cap" is WRONG** - she applies 19.94 stacks a game into a payoff capped at 8 and a bonus capped at +25% (the TREACHERY signature exactly), but halving the grant COST 12.8 points and doubling it gained 12.0. The stacks are consumed as fast as they arrive: **her engine is undersupplied, not overflowing.**
0-THE-ZOO-DECKS-ARE-12TH-14TH-15TH-AT-ZOO'S-JOB. Ranked by beating RAMP in neutral cells: `huldra_v1` 76.1 (CONTROL), `gullinbursti_v1` 76.1, `jormungandr_v2` 73.6, `gullinbursti_v2` 61.9, `nidhoggr_v1` 60.2, `nidhoggr_v2` 58.0, `hel_v1` 53.7 ... then `jormungandr_v1` **39.4**, `hraesvelgr_v1` **33.6**, `sleipnir_v1` **30.5**. **Eleven decks prey on RAMP harder than the best ZOO deck.** By 0-cost share (the velocity signature) the widest decks are `ratatoskr_v1` **55%** (CONTROL), `nidhoggr_v2` 50%, `hel_v1` 45%, against ZOO's 44/42/25%. **On both signatures the reclassification candidates are `nidhoggr_v1` and `nidhoggr_v2`** - but they are the 4th/5th strongest decks, so moving them makes ZOO the strongest role. **Recommendation: fix the conversion premium FIRST, relabel second** - the wheel's failure is mechanical, not clerical.

0-EVERY-DECK-HAS-THE-SAME-ECONOMY (ticket 87 - [research/what-is-an-archetype.md](research/what-is-an-archetype.md)). **`energy: 2` on 14 of 15 species, `cardDraw: 3` on 12 of 15.** Nothing grows, nothing is starved - **there is no ramp in the game because there is nothing to ramp TO.** And the budget rule `50 x Energy - 10` prices the resources against each other: per turn **2 Energy buys 90 power, three 0-cost cards buy 30** - energy is worth 3x cards by our own rule, so **the deck built to spend cards is spending the cheap resource.** The premium for going big is only +12.5% at 2e / +16.7% at 3e, and no deck reaches 3e reliably on a 2-Energy frame. **Both halves of the wide-vs-big trade-off are inert.**
0-NO-ROLE-MATCHES-ITS-NAME (ticket 87, `scratch/shape.ts`, 18 decks x 120 games). Role averages - cards/turn, slope (late/early damage), spike (best turn as % of a health bar): **ZOO 3.63 / 0.71 / 41.9%, CONTROL 3.53 / 0.67 / 30.7%, BURST 2.71 / 0.89 / 37.5%, RAMP 2.01 / 0.95 / 31.2%.** ZOO is NOT wider than CONTROL; **the widest deck in the game is `ratatoskr_v1` at 5.91 cards/turn** (CONTROL today, 3 Energy / 4 draw / 55% free - Henry: *"ratatoskr should be a zoo deck"*, and it is the only deck with zoo's resource profile). **BURST spikes LESS than ZOO** - "burst" describes the hand, not the turn. **RAMP is flat at 0.95**; the most back-loaded deck is `nidhoggr_v1` (1.23x), a BURST deck. The one honest label is `hel_v2`: slope 0.38, spike 50.7%.
0-COMPUTE-THE-ROLE-DO-NOT-ASSERT-IT. Proposed tags with tests, non-exclusive: **WIDE** >= 3.5 cards/turn, **TALL** best turn >= 40% of a health bar, **FAST** slope <= 0.70, **LATE** slope >= 1.10, **DENIAL** lowers opponent damage/turn >= 15% (not yet instrumented). Renames argued for: **BURST -> TALL, ZOO -> WIDE, RAMP -> LATE, CONTROL -> DENIAL.** Six levers to make roles real, in leverage order: **(1) vary `energy`/`cardDraw` per species** - `ratatoskr`'s 3/4 is the natural experiment already in the repo; **(2) let Energy GROW** or ramp cannot exist; (3) steepen the big-card premium; (4) raise what a CARD is worth (ticket 86's payoff route is the safe version); (5) give CONTROL a denial axis - **`huldra_v1` deals 1.1 damage per card and beats the sustain cluster 76%, so denial works and is simply unbuilt**; (6) make TIME legible to cards - there is no turn-indexed effect anywhere in the pool.

## Process

- **Sequencing (Henry):** every remaining deck gets a FIRST PASS before any deep tuning. Process + lightweight gate: [research/first-pass-process.md](research/first-pass-process.md). 10-80-10 division: design/analysis on the primary model, mechanical work delegated via prompts with exact anchors + tolerances + STOP conditions.
- Deck rulebook (ticket 04): 8 cards base (up to 12), ≤2 copies, three tiers (None/neutral = no STAB by design; element-shared; 30–50% OS-specific), one 3e payoff where the OS wants it. Each element pass retires its 0e poke twin into Tackle (`water_slap` id).
- **Henry's rules:** numbers move in 5s; ONE change per sim; he reviews ALL deck lists and card changes before registry commits; reports need a card appendix (in-game text for every referenced card) + archetype framing.
- **Repo law:** CRLF for engine `.ts` and `docs/wayfinder`; LF for tests, `src/debug`, and JSON. `programs.json` round-trips byte-exact under `json.dumps(d, indent=4, ensure_ascii=False)` with no trailing newline. **`hooks.json` does NOT** — its inline arrays expand on a JSON round-trip, so edit it surgically as text. If `git diff --stat` shows a whole file changed, you converted its endings; fix before committing.
- One commit per ticket, author `Henry Dunphy <hdunphy15@gmail.com>` via `git -c user.name=... -c user.email=... commit --author=...`. Never stage `package-lock.json` or `node_modules`. Balance re-baselines commit **only** `docs/balance/balance_report.json` — the CSVs are deliberately stale (precedent `868dd9a`).
- If git locks are un-deletable ("Operation not permitted"), `mv` them into `_to_delete/git-locks/`.

## Design notes captured mid-flight

- **Water daemon gap (Henry):** Water shipped with no daemon card — add one in a later Water polish pass.
- **Air pricing principle (Henry):** both Air OSes generate free value (Str stacks / free tokens), so Air cards may want to be DELIBERATELY under-curve — test during the deep pass, don't "fix" under-curve Air cards to budget blindly.
- **Enabler-first is proven three times** (kraken OS decomposition, MOMENTUM_DRIVE, Burn overflow). When a §2.3 gap resists the card knobs, suspect a capped or under-paying OS — or a mechanic the auditor cannot price — before touching card economics.
- **Anti-deadlock margin to watch (ticket 23):** capped Weakened × Sharp now floors to 1 damage instead of 2. The 25% cap still prevents a true stall, but re-check that case if the pace divisor is ever raised again.
